# Handoff 21 — 2026-07-11 (session 21: session-20 push + Mon/Wed/Fri cadence + deep-dive polish pair)

Continues [HANDOFF_20.md](HANDOFF_20.md). Evergreen orientation in [HANDOFF.md](HANDOFF.md); rolling status/roadmap in [PLAN.md](../PLAN.md); structural map in [PROJECT_SUMMARY.md](../PROJECT_SUMMARY.md); design spec in [SPEC.md](../SPEC.md).

## What session 21 did

On branch **`feat/session-21-cadence-deepdive`** (off `main` @ `57abb8a`), worktree `.claude/worktrees/session-21-cadence-deepdive`. Pushed, **not merged**.

Session start: **the repo disagreed with the handoff prompt and the repo won — again** (third session in a row): session 20 was found already ff-merged into local `main` (@ `57abb8a`) but never pushed (the prompt said "pushed NOT merged" with main at `3cdaf1a`). Flagged to the user; they approved and `main` @ `57abb8a` was pushed **Saturday 07-11 ~21:40 UTC — before Mon 07-13 10:00 UTC**, so the week's production data carries the fundamentals blocks. Session ran on **Saturday 07-11** (third session that day), so HANDOFF_20 steps 2–3 (Mon 07-13 run, Fri 07-17 retro) stay future — the latest production run is still the verified 07-10 11-flip run. Reddit creds still 0. Session-20 worktree clean.

The slice (user pick: **lower run frequency + the deep-dive polish pair**):

1. **Cadence Mon/Wed/Fri** (user decision from four options): [.github/workflows/run_recommendations.yml](../.github/workflows/run_recommendations.yml) cron `0 10 * * 1-5` → **`0 10 * * 1,3,5`** (still 10:00 UTC = 12:00 CEST Madrid). The inline comment documents the decision and the hard constraint: **Friday must stay in the schedule — it triggers the weekly retrospective** (`src/main.py` runs the retro on Fridays only). ~−40% API bill (≈$0.29/week at $0.097/run). README + PROJECT_SUMMARY cadence text synced (SPEC left alone — historical design doc, already stale at "2x/día" through prior cadence changes).
2. **Deep-dive "Action flips" annotations** ([grafana/ticker_deep_dive_dashboard.json](../grafana/ticker_deep_dive_dashboard.json)): a second entry in the dashboard-level `annotations` array — schema-v2 `AnnotationQuery` written against Grafana's canonical `types.spec.gen.ts` (`AnnotationQuerySpec`: `enable/hide/iconColor/name/builtIn/filter/query`), with **`filter: {exclude: false, ids: [5]}`** so the orange markers render only on the price-history panel. The rawSql joins each recommendation to its ticker's immediately-preceding run where the action changed (identical semantics to digest panel-9), filtered to `t.symbol IN ($ticker)` + `$__timeFilter(r2.generated_at)`, returning `time` (epoch via `UNIX_TIMESTAMP`), `text` ("MU: WATCH → BUY (conf 72%)"), `tags` (`symbol,action`). Panel-5's description and panel-4's how-to-read now explain the markers (unchanged daily calls are NOT marked — only flips).
3. **Predictions → deep-dive data link** ([grafana/predictions_dashboard.json](../grafana/predictions_dashboard.json)): a `links` property on panel-1's existing symbol-column override — `{"title": "Open ticker deep dive", "url": "/d/DEEP_DIVE_UID/ticker-deep-dive?var-ticker=${__data.fields.symbol}", "targetBlank": false}`. **`DEEP_DIVE_UID` is a deliberate placeholder** (user choice — they didn't fetch the uid this session): schema-v2 exports carry no uid, Grafana assigned one when the user imported the deep-dive in session 20. One-time fix: replace the placeholder with the segment after `/d/` in the deep-dive's URL — either in the JSON before importing, or in Grafana's data-link editor after. Documented in panel-1's description and the how-to-read panel.
4. **Docs**: README + PROJECT_SUMMARY cadence, PLAN refresh, this handoff.

## Validation evidence

- **YAML**: parses; cron `0 10 * * 1,3,5` matches its comment. Workflow-only rule applies: no dry-run needed (no pipeline code touched anywhere this session).
- **pytest: 84 passed** (no test changes — nothing under `src/` or `tests/` was modified). Note: worktrees carry no `.venv`; run pytest with the main checkout's interpreter (`/home/guillo/Git/stock-recommendations/.venv/bin/python -m pytest tests/ -q` from the worktree).
- **Annotation rawSql green live** (collation init_command; `$ticker` → quoted CSV; `$__timeFilter` → literal BETWEEN): multi `'MU','SOLS','VWRL.AS'` → **7 flips** (the known 07-10 production flips appear: MU WATCH→BUY, SOLS ping-pong), single `'AAPL'` → 1 row (AVOID→WATCH), unknown symbol → 0 rows without error. Columns verified: epoch int / text / tags.
- **Both JSONs parse**; annotation shape checked field-by-field against the v2 spec; the predictions diff is surgical (description + one override property + how-to-read).
- **Not done (by design): live render.** The annotation and the data link are first-of-their-kind for this repo's dashboards — the merge gate is a first-import eyeball (see step 1 below).

## Invariants (don't break)

All of HANDOFF_20's invariants stand (never write to stock-snapshots tables; `--dry-run` = no writes, real API; Spanish prompts; no persisted placeholders; 30d headline horizon; `get_latest_actions` before step 6a; ETF fetch only for `quote_type == "ETF"`, fundamentals fetch only for `quote_type == "EQUITY"` — mutually exclusive; `dividendYield` stays in Yahoo percent units, margins/growth are fractions; `_build_fundamentals`/`_build_etf_info` → None when unknown; deep-dive `$ticker` keeps `multi`+`includeAll` and `IN ($ticker)` filters; price-history panel stays `format: time_series`; panel-6 ETF bucket keys on `t.quote_type`; schema-v2 dashboard rules — never emit `timeSettings.weekStart/nowDelay/quickRanges` or `vizConfig.spec.version`; batch custom_ids `[A-Za-z0-9_-]`; worktree-per-session ritual; user only sees the final message). New this session:

- **Friday stays in the cron.** Any future cadence edit must keep a Friday run or the weekly retrospective (and digest panel-12) silently dies. The constraint is commented inline in the workflow.
- **The flips annotation must keep `filter.ids: [5]`** (price-history panel only) and return `time` as epoch seconds — schema-v2 `AnnotationQuery` under `annotations[]`, not per-panel.
- **`DEEP_DIVE_UID` is a placeholder, not a bug** — do not "fix" it to a guessed uid; only the user's Grafana knows the real one. If a future session gets the uid, replace it in `predictions_dashboard.json` and simplify the two doc mentions.
- **Flip-count comparisons change meaning under the new cadence**: with Mon/Wed/Fri, "vs previous run" spans 2-day gaps, so per-run flip counts will read higher than the daily-cadence baselines (11 on 07-10; dry-runs 7–14). Compare trend-vs-itself, not against daily-era absolutes.

## State of play / caveats

- **`main` = `57abb8a` (session 20 fundamentals merged + pushed 2026-07-11 ~21:40 UTC — before the Mon 07-13 run, so the week's production carries the Fundamentales blocks)**. Session 21 is on `feat/session-21-cadence-deepdive`, pushed, **awaiting user merge**.
- **Merge gate for s21 is a dashboard eyeball + one manual edit**: the user imports `ticker_deep_dive_dashboard.json` (orange flip markers on the price chart, hover text) and `predictions_dashboard.json` (symbol click-through) — and **replaces `DEEP_DIVE_UID`** in the predictions data link with the deep-dive's real uid. If the merge lands **before Mon 07-13 10:00 UTC**, next week is already Mon/Wed/Fri; if after, Monday still runs on the daily cron (harmless — just one extra run).
- **The Mon 2026-07-13 run** is the first with s16+s17+s18+s20 live: expect 63 ok, cost ≈ **$0.097**, flip count vs 11 on 07-10 (the verdict on session 16; flip-stability round 2 only if flips stay ≥10 with same-day reversals), **and reasonings citing fundamentals** (P/E, margins, growth in the Spanish text) for EQUITY tickers.
- **First real retrospective row lands Friday 2026-07-17** (log line + `weekly_retrospectives` row + digest panel-12).
- **ETF hit rate 37% (486 outcomes) is the s18 baseline**; watch track-record panel-6. First ETF-informed calls mature ~mid-August; first fundamentals-informed calls mature **~2026-08-13** at 30d.
- **Reddit still dark** (`grep -c '^REDDIT_' .env` = 0, 2026-07-11). Batched Reddit sentiment stays gated.
- **Remaining committed backlog** (PLAN roadmap): benchmark-relative + dividend-adjusted grading (needs user decisions + re-grade sign-off; best ~2026-08-15 when 90d outcomes near maturity). Also available: per-run cost telemetry in the DB (migration — sign-off), fundamentals persistence on rec rows (migration — sign-off; unlocks fundamentals-vs-verdict analysis), portfolio lens dashboard, flip-stability round 2 (only if the week says so), batched Reddit sentiment (if creds).
- Carried: 252 pre-price_checks matured candidates ungradeable; `price_snapshots` stale since 2026-05-22; price_checks gap 06-30→07-08 permanent; 90d/365d series fill from ~2026-08-15 / 2027-05-17; deep-dive price history starts 2026-06-12. **New:** from the first Mon/Wed/Fri week onward, `price_checks` gains rows only 3×/week — outcome grading tolerates this (nearest row in the horizon window) but exit prices may be up to 2 days off the exact maturity date.
- **Local runs:** `env -u ANTHROPIC_API_KEY` (empty shell var shadows `.env`). Worktrees have no `.venv` — use the main checkout's (`/home/guillo/Git/stock-recommendations/.venv/bin/python`). Ad-hoc DB scripts: `load_dotenv('/home/guillo/Git/stock-recommendations/.env')`, env vars `DB_HOST/DB_PORT/DB_USER/DB_PASS/DB_NAME` (don't forget the port), init_command `SET collation_connection = utf8mb4_unicode_ci` when validating dashboard rawSql.
- **User timezone: Europe/Madrid.**

## Detailed TODO for session 22 (step-by-step; follow in order)

**Step 0 — Orient.** Read this file and [PLAN.md](../PLAN.md). From the main checkout (`/home/guillo/Git/stock-recommendations`): `git pull`, then `git log --oneline -3 --decorate` and compare against the prompt you were given — **if they disagree, the repo wins; say so to the user before doing anything** (it has now happened three sessions running: the previous session's branch keeps being found merged-but-unpushed, or pushed when the prompt says not). `git worktree list`; check the newest worktree's `git status --short` for interrupted work.

**Step 1 — Merge gate for session 21.** Ask the user (AskUserQuestion) to: (a) import `grafana/ticker_deep_dive_dashboard.json` from the s21 branch/worktree and confirm **orange flip markers** appear on the price-history panel (pick a flippy ticker like MU or SOLS, time range covering July; hover shows "old → new (conf N%)"); (b) import `grafana/predictions_dashboard.json`, **replace `DEEP_DIVE_UID`** in the symbol column's data link (Panel edit → Field overrides → symbol → Data links; the real uid is the segment after `/d/` in the deep-dive's URL — or edit the JSON before importing and re-import), and confirm clicking a symbol lands on the deep-dive filtered to that ticker. On approval: `git checkout main && git merge --ff-only feat/session-21-cadence-deepdive && git push`. If before Mon 07-13 10:00 UTC, the new Mon/Wed/Fri cadence applies immediately — say so. If the user got the real uid, offer to commit it into the JSON (replacing the placeholder) so future imports come pre-wired.

**Step 2 — Verify the week's production runs (Mon 07-13 onward).** `gh run list --workflow=run_recommendations.yml --limit 5`; per scheduled run check: 63 ok / 0 failed; cost ≈ $0.097; the `Action flips vs previous run:` count vs 11 on 07-10; **spot-check 2–3 reasonings for fundamentals citations** (P/E, márgenes, crecimiento — EQUITY tickers only; ETFs should cite exposure/costs instead). If s21 merged before Monday, also confirm **no runs fire on Tue/Thu**. Remember the cadence caveat: post-s21 flip counts span 2-day gaps — judge the trend, not the daily-era absolute. Record counts in PLAN.

**Step 3 — First production retrospective (on/after Friday 2026-07-17).** After Friday's run: the log shows the retrospective generation + upsert lines; `SELECT week_start, LEFT(retrospective, 200) FROM weekly_retrospectives;` has the week's row; the user reads digest panel-12. Note whether its flip commentary matches step 2's counts.

**Step 4 — Check Reddit creds** (`grep -c '^REDDIT_' .env`). If >0: add the three GitHub Actions secrets, run one real cycle, verify `reddit_mentions` + `trending_tickers` fill and digest panel-14 renders; batched Reddit sentiment becomes buildable.

**Step 5 — Pick the next slice with the user** (AskUserQuestion). Suggested order:
1. **Per-run cost telemetry in the DB**: persist the run's token/cost line (a small table keyed on run timestamp, or columns on `daily_market_summary`) — needs a **migration (user sign-off)**; then a cost-trend panel. With the cadence change just landed, a dashboardable cost trend is the natural way to confirm the −40%.
2. **Fundamentals persistence on recommendation rows** (compact JSON column — migration, sign-off): prerequisite for the fundamentals-vs-verdict slice when s20-informed calls mature ~08-13.
3. **Benchmark-relative + dividend-adjusted grading**: needs user decisions (benchmark choice; re-grade or not — session-14 rule) and total-return data; best once 90d outcomes near maturity (~2026-08-15). Do the two together.
4. Others if asked: portfolio lens dashboard, flip-stability round 2 (only if step 2 says so), batched Reddit sentiment (only if creds), retry-once on transient yfinance timeouts (cheap, stub-tested).

**Step 6 — Validate** the standard way: `env -u ANTHROPIC_API_KEY /home/guillo/Git/stock-recommendations/.venv/bin/python -m pytest tests/ -q` (expect 84+); full dry-run (`… -m src.main --dry-run`, 63 ok / 0 failed, `--force-retro` if the retro path was touched) **only if pipeline code changed**; re-extract and run any touched dashboard rawSql live with the collation init_command (substitute `$ticker` with a quoted CSV on deep-dive queries, `$__timeFilter(...)` with a literal BETWEEN); migrations only with user sign-off (test on a throwaway table name first if unsure). Workflow-only changes: YAML parses + comment matches — no dry-run.

**Step 7 — Close out per the ritual.** Update PLAN.md; write `handoffs/HANDOFF_22.md` with a complete copy-pasteable next prompt + a detailed TODO an older model can follow + fresh suggestions; commit; push the branch (no merge unless asked); print the full next prompt in chat.

## Fresh suggestions (beyond the committed backlog)

- **Commit the real deep-dive uid once known** (step 1 offers this): with the uid in the repo, the predictions link survives re-imports and the DEEP_DIVE_UID caveat disappears from three doc locations.
- **Retire the daily-era flip baselines**: once two Mon/Wed/Fri weeks exist, recompute a fresh flips-per-run baseline (digest panel-13) and note it in PLAN — comparisons against 11/07-10 become apples-to-oranges after the cadence change.
- **Fundamentals-vs-verdict slice** (carried from HANDOFF_20): hit-rate-by-P/E-band or by-dividend-payer on the track record, once entry fundamentals are persisted (suggestion 2 in step 5) and s20-informed calls mature ~08-13.
- **Retry-once on transient yfinance timeouts** (carried): the s20 probe hit one 30s curl timeout (RGTI); each enrichment fetch degrades to None correctly, but a single retry would cut silent data gaps. Cheap, test with a stubbed raiser.

## Prompt for the next session (copy-paste exactly)

> Read handoffs/HANDOFF_21.md and PLAN.md before doing anything — HANDOFF_21 has the detailed step-by-step TODO for session 22; follow it in order, and first cross-check this prompt's claims against git log (if they disagree, the repo wins — say so; it has happened three sessions running: s21 found s20 merged-but-unpushed). Context: main is at 57abb8a (session 20 fundamentals merged + pushed Sat 2026-07-11 ~21:40 UTC — before the Mon 07-13 10:00 UTC run, so the week's production carries the Fundamentales blocks; spot-check reasonings for P/E/margin/growth citations after Monday). Session 21 (Saturday 2026-07-11, branch feat/session-21-cadence-deepdive, pushed NOT merged) delivered the lower-run-frequency backlog item + the deep-dive polish pair: cron 0 10 * * 1,3,5 (user decision Mon/Wed/Fri; Friday MUST stay — it triggers the weekly retrospective; ~−40% API bill; README/PROJECT_SUMMARY synced), an "Action flips" schema-v2 AnnotationQuery on the ticker deep-dive (digest panel-9 flip semantics scoped to $ticker, filter.ids [5] so orange markers render only on the price-history panel, hover "MU: WATCH → BUY (conf 72%)"; rawSql validated live multi/single/empty), and a data link on the predictions symbol column to /d/DEEP_DIVE_UID/ticker-deep-dive?var-ticker=${__data.fields.symbol} — DEEP_DIVE_UID is a deliberate placeholder (schema-v2 exports carry no uid; the user must replace it with the deep-dive's real uid post-import — documented in the how-to-read panel). Validation: pytest 84 passed; YAML + both JSONs parse; annotation rawSql green live; no pipeline code touched so no dry-run (workflow/dashboard-only rule); live render NOT done — that's the merge gate. Steps: (1) merge gate — user imports both dashboards (flip markers on the price chart; symbol click-through), replaces DEEP_DIVE_UID with the real uid (offer to commit it if provided), then ff-merge feat/session-21-cadence-deepdive to main and push; if before Mon 07-13 10:00 UTC the Mon/Wed/Fri cadence applies immediately — say so; (2) verify the week's production runs (Mon 07-13 onward: 63 ok, cost ≈$0.097, flip trend vs 11 on 07-10 — but note post-s21 flips span 2-day gaps so counts read higher; fundamentals citations in EQUITY reasonings; if s21 merged, confirm no Tue/Thu runs); (3) on/after Fri 07-17 verify the first production weekly retrospective (log line + weekly_retrospectives row + digest panel-12); (4) check Reddit creds (grep -c '^REDDIT_' .env — 0 as of 07-11). Then pick the next slice with the user from HANDOFF_21 step 5 (suggested: per-run cost telemetry in the DB with migration sign-off — the natural way to confirm the −40%; also: fundamentals persistence on rec rows for the fundamentals-vs-verdict slice ~08-13, benchmark-relative + dividend-adjusted grading ~08-15, portfolio lens, flip-stability round 2 only if the week says so, batched Reddit sentiment if creds). Create the session worktree + branch (feat/session-22-<slug>) once the slice is known, confirm the task list with the user, batch the work, validate (pytest expect 84+ using the main checkout's .venv — worktrees have none; full dry-run 63 ok only if pipeline code changed; rawSql with SET collation_connection = utf8mb4_unicode_ci if dashboards touched — substitute $ticker with quoted CSV on deep-dive queries; migrations only with user sign-off), and close out per the ritual: update PLAN.md, write handoffs/HANDOFF_22.md with the complete next prompt + a detailed TODO an older model can follow + fresh suggestions, push without merging, and print the full next-session prompt in the chat.
