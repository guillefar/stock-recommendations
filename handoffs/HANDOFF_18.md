# Handoff 18 — 2026-07-11 (session 18: ETF awareness + session-17 merge)

Continues [HANDOFF_17.md](HANDOFF_17.md). Evergreen orientation in [HANDOFF.md](HANDOFF.md); rolling status/roadmap in [PLAN.md](../PLAN.md); structural map in [PROJECT_SUMMARY.md](../PROJECT_SUMMARY.md); design spec in [SPEC.md](../SPEC.md).

## What session 18 did

On branch **`feat/session-18-etf-info`** (off `main` @ `1e51d60`), worktree `.claude/worktrees/session-18-etf-info`. Pushed, **not merged**.

Session start: **merged session 17** (`feat/session-17-token-trims` ff → `main` @ `1e51d60`, pushed, user-approved) before the Mon 07-13 10:00 UTC run — the week's production data reflects the trimmed prompts. Also corrected the user's timezone (Spain/Madrid, **not** Argentina — a scheduler message had guessed ART; the production cron was already Madrid-based, nothing to change in the repo) and recorded the **user-selected long-term backlog** (see PLAN roadmap): fundamentals in prompts, benchmark-relative grading, dividend-adjusted returns, lower run frequency.

The slice (user ask: "can we add info about ETF?"):

1. **Why it matters**: 27 of the 63 active tickers are ETFs that ran on technicals alone — no sector, news or earnings ever reached their prompts. The new sector split quantifies the damage: **486 graded 30d outcomes in the previously-hidden ETF bucket at a 37% hit rate** — the system's worst segment (Technology: 70%).
2. **Detection is DB-driven, zero migrations**: the read-only stock-snapshots `tickers` table already carries `quote_type` (27 ETF / 33 EQUITY / 1 INDEX among actives). `get_active_tickers` ([src/db.py](../src/db.py)) now returns it; only actual ETFs pay the yfinance fetch.
3. **`fetch_etf_info` + `_build_etf_info`** ([src/collectors/prices.py](../src/collectors/prices.py)): yfinance `funds_data` → `{family, category, expense_ratio, top_holdings (5), sector_weights (positive only)}`; **None on any error / no fund data / all-empty** (optional enrichment, never fails the ticker). **Yahoo serves expense ratio 0.0 for many UCITS ETFs where the truth is unknown — normalized to None, never rendered as "0.00%".**
4. **"Perfil del ETF" prompt block** ([src/analysis/claude_client.py](../src/analysis/claude_client.py) `_etf_block`): gestora / categoría / ratio de gastos / top-5 posiciones (e.g. "ASML.AS 11.1%") / top-5 sectores by weight, headed by the instruction *"fondo diversificado: evalúa la tesis por su exposición sectorial/regional y sus costes, no como una acción individual"*. Omitted for stocks and unknown funds — same optional-block pattern as news/earnings. Wired in [src/main.py](../src/main.py) step 6a (`etf_info` in the prepared payload).
5. **Retro sector exposure** ([src/analysis/retrospective.py](../src/analysis/retrospective.py)): ETFs bucket as "ETF" instead of flooding "(sin sector)".
6. **Track-record panel-6** (hit rate by sector): sector expression now `COALESCE(NULLIF(t.sector_disp,''), CASE WHEN t.quote_type='ETF' THEN 'ETF' END, '(unknown)')` (applied programmatically; panel description + how-to-read panel updated). Because ETF-ness is a ticker property, **all historical ETF outcomes bucket immediately** — a per-recommendation marker would have taken 30 days to appear.

## Validation evidence

- **pytest: 75 passed** (68 + 7 new in [tests/test_etf_info.py](../tests/test_etf_info.py): `_build_etf_info` normalization / zero-expense-ratio→None / empty→None, prompt block render + omission, main() fetches only for ETFs and wires `etf_info` to the batch, retro ETF bucket).
- **Live collector smoke**: XESC.DE → DWS Investment S.A., 0.09% expense ratio, ASML.AS 11.1% top holding, financial_services 26% top sector; rendered block eyeballed.
- **Live DB**: `get_active_tickers` still 63 rows (27 ETFs); new panel-6 SQL run against the live DB with the collation init_command — ETF bucket = 486 outcomes / 37% hit rate; '(unknown)' shrinks 54 rows / 78%.
- **Full dry-run through the real batch API** (2026-07-11 ~18:47 UTC+2, exit 0): **63 ok / 0 failed**, cost **$0.0973** vs the $0.0912 session-17 baseline — the 27 ETF blocks add ~5.3K batch-input tokens (86.6K → 91.9K, ≈195 tokens/block, ≈+$0.006/run); usage 10.4K plain-input / 2.2K plain-output / 91.9K batch-input / 11.9K batch-output. **7 flips** (POET HOLD→SELL, AMPX HOLD→SELL, LDEU.L WATCH→BUY, VWRL.AS WATCH→BUY, MU AVOID→WATCH, CLS AVOID→WATCH, SOLS BUY→WATCH — AMPX/VWRL.AS/MU/SOLS repeat the session-16/17 consistent set), summary MIXED.

## Invariants (don't break)

All of HANDOFF_17's invariants stand (never write to stock-snapshots tables; `--dry-run` = no writes, real API; Spanish prompts; no persisted placeholders; 30d headline horizon; `get_latest_actions` before step 6a; first-sentence-only summary reasonings; retro maturity = `generated_at + 30d`; schema-v2 dashboard rules; batch custom_ids `[A-Za-z0-9_-]`; worktree-per-session ritual; user only sees the final message). New this session:

- **`fetch_etf_info` is only called for `quote_type == "ETF"`** — stocks must never pay the funds_data fetch (and `tickers.quote_type` is read-only stock-snapshots data).
- **`_build_etf_info` returns None when nothing is known** (and treats expense ratio 0.0 as unknown) — the prompt block must stay omitted rather than render empty/bogus lines.
- **The ETF bucket in panel-6 keys on `t.quote_type`, not on recommendation JSON** — keep it that way so history stays covered.

## State of play / caveats

- **`main` = `1e51d60` (session 17 merged, pushed)**; session 18 is on `feat/session-18-etf-info`, pushed, **awaiting user merge**. After merge the user re-imports **track_record_dashboard.json** (panel-6 rawSql changed) — plus predictions + track-record from the s17 merge if not yet done.
- **The Mon 2026-07-13 run** carries flip-stability (s16) + token trims (s17); if session 18 merges before Monday it also carries the ETF blocks (cost then compares to this handoff's dry-run, not $0.0912).
- **ETF hit rate 37% (486 outcomes) is the baseline** the composition block is trying to move — it will move slowly (30d maturity lag; first ETF-informed calls mature ~mid-August 2026).
- **First real retrospective row lands Friday 2026-07-17**; panel-12 empty until then; panel-14 empty until Reddit creds.
- **Reddit still dark** (`grep -c '^REDDIT_' .env` = 0, 2026-07-11). Batched Reddit sentiment stays gated.
- **Committed backlog**: the **long-term backlog** (4 user-selected items, PLAN roadmap section) — fundamentals in prompts, benchmark-relative grading, dividend-adjusted returns, lower run frequency. Grading changes among these need an explicit user decision + re-grade (session-14 rule). Plus the old suggestions: per-run cost telemetry in the DB, flip-stability round 2 (only if production flips stay ≥10), portfolio lens dashboard, batched Reddit sentiment (gated).
- Carried: 252 pre-price_checks matured candidates ungradeable; `price_snapshots` stale since 2026-05-22; price_checks gap 06-30→07-08 permanent; 90d/365d series fill from ~2026-08-15/2027-05-17.
- **Local runs:** `env -u ANTHROPIC_API_KEY` (empty shell var shadows `.env`). Ad-hoc DB scripts: `load_dotenv('/home/guillo/Git/stock-recommendations/.env')`, env var is `DB_PASS`, add `SET collation_connection = utf8mb4_unicode_ci` as init_command when validating dashboard rawSql.
- **User timezone: Europe/Madrid.** Any schedule/cron reasoning uses Madrid time (CET winter / CEST summer).

## Detailed TODO for session 19 (step-by-step; follow in order)

**Step 0 — Orient.** Read this file and [PLAN.md](../PLAN.md). From the main checkout (`/home/guillo/Git/stock-recommendations`): `git pull`, then `git log --oneline -3` and compare against what the prompt you were given claims — **if they disagree, the repo wins; say so to the user before doing anything**. Also run `git worktree list` and check the newest worktree's `status --short` for interrupted work.

**Step 1 — Merge gate for session 18.** Ask the user (AskUserQuestion) to approve merging `feat/session-18-etf-info` (ETF prompt block + sector-panel ETF bucket; no migrations; 75 tests green; dry-run evidence above). On approval, from the main checkout: `git checkout main && git merge --ff-only feat/session-18-etf-info && git push`. **The user then re-imports `track_record_dashboard.json` from `main`** (panel-6 rawSql changed — importing the stale copy is the classic failure mode, see HANDOFF_13).

**Step 2 — Verify the week's production runs.** `gh run list --workflow=run_recommendations.yml --limit 5`; on each scheduled run's log check: 63 ok / 0 failed; the cost line (s17 trims: expect ≈$0.09; + s18 ETF blocks if merged: slightly above that, compare to this handoff's dry-run figure); the `Action flips vs previous run:` count — the Mon 07-13 → Fri 07-17 trend (vs 11 on 07-10) is the production verdict on session 16's reinforcement. Settling = counts dropping toward low single digits without same-day reversals. Record the counts in PLAN. If flips stay ≥10 with reversals, **flip-stability round 2** becomes a candidate slice.

**Step 3 — First production retrospective (on/after Friday 2026-07-17).** After Friday's run: `gh run view <id> --log | grep -i retrospective` (expect "Generating weekly retrospective..." + the upsert line); `SELECT week_start, LEFT(retrospective, 200) FROM weekly_retrospectives;` has the week's row; the user reads digest panel-12. Note whether its flip commentary matches step 2's counts.

**Step 4 — Check Reddit creds** (`grep -c '^REDDIT_' .env`). If >0: add the three GitHub Actions secrets, run one real cycle, verify `reddit_mentions` + `trending_tickers` fill and panel-14 renders; batched Reddit sentiment becomes buildable.

**Step 5 — Pick the next slice with the user** (AskUserQuestion). The **long-term backlog** (user-selected 2026-07-11) is the committed pool — suggested order:
1. **Fundamentals in the per-ticker prompt** (stocks): P/E, dividend yield, margins, revenue growth via yfinance (`Ticker.info` fields; probe availability first — European tickers are patchy). Same optional-block pattern as news/earnings/ETF. Self-contained, no grading impact, directly serves the months-long thesis.
2. **Lower run frequency** (daily → 2–3×/week): tiny change (cron line), but ask the user for the cadence; keep a Friday run for the retro. Halves-to-thirds the API bill.
3. **Benchmark-relative + dividend-adjusted grading at 90d/365d**: bigger — needs a user decision on the benchmark (SPY? VWRL.AS? per-region?), total-return data (yfinance auto-adjusted closes), and whether to re-grade existing rows (session-14 rule: user sign-off + delete-and-regrade). Do these two together; they touch the same evaluator code. Best started once 90d outcomes begin maturing (~2026-08-15).
4. Others if asked: per-run cost telemetry in the DB, portfolio lens dashboard, flip-stability round 2 (only if step 2 says so).

**Step 6 — Validate** the standard way: `env -u ANTHROPIC_API_KEY .venv/bin/python -m pytest tests/ -q` (expect 75+); full `env -u ANTHROPIC_API_KEY .venv/bin/python -m src.main --dry-run` (63 ok / 0 failed; `--force-retro` if the retro path was touched); re-extract and run any touched dashboard rawSql with the collation init_command; migrations only with user sign-off.

**Step 7 — Close out per the ritual.** Update PLAN.md; write `handoffs/HANDOFF_19.md` with a complete copy-pasteable next prompt + a detailed TODO an older model can follow + fresh suggestions; commit; push the branch (no merge unless asked); print the full next prompt in chat.

## Prompt for the next session (copy-paste exactly)

> Read handoffs/HANDOFF_18.md and PLAN.md before doing anything — HANDOFF_18 has the detailed step-by-step TODO for session 19; follow it in order, and first cross-check this prompt's claims against git log (if they disagree, the repo wins — say so). Context: main is at 1e51d60 (session 17 token-trims merged + pushed 2026-07-11, before the Mon 07-13 run). Session 18 (2026-07-11, branch feat/session-18-etf-info, pushed NOT merged) delivered ETF awareness: get_active_tickers now returns tickers.quote_type (column already existed — no migration; 27 of 63 actives are ETFs), ETF tickers get a yfinance funds_data fetch (fetch_etf_info — family, expense ratio with Yahoo's bogus 0.0 normalized to None, top-5 holdings, sector weights; stocks skip it entirely), the per-ticker prompt renders a "Perfil del ETF" block instructing the model to judge the fund by exposure and costs, the weekly retro buckets ETFs as "ETF", and track-record panel-6 groups all historical ETF outcomes under 'ETF' via quote_type — revealing the baseline the block must beat: 37% hit rate on 486 graded ETF outcomes (worst segment; Technology is 70%). Validation: pytest 75 passed; live SQL + collector smokes green; full dry-run numbers in HANDOFF_18. Also done: user timezone corrected to Spain/Madrid (repo cron was already right), and the user selected a four-item long-term backlog (fundamentals in prompts, benchmark-relative grading, dividend-adjusted returns, lower run frequency — see PLAN roadmap). Steps: (1) merge gate — user approval, then ff-merge session 18 to main and push; user re-imports track_record_dashboard.json from main after (panel-6 rawSql changed); (2) verify the week's production runs (Mon 07-13 onward: 63 ok, cost ≈$0.09 + ETF-block delta if s18 merged, flip trend vs 11 on 07-10 — the verdict on session 16; round 2 only if flips stay ≥10); (3) on/after Fri 07-17 verify the first production weekly retrospective (log line + weekly_retrospectives row + panel-12); (4) check Reddit creds (grep -c '^REDDIT' .env — 0 as of 07-11). Then pick the next slice with the user from HANDOFF_18 step 5 (suggested order: fundamentals-in-prompt, lower run frequency, benchmark-relative + dividend-adjusted grading once 90d outcomes near maturity ~08-15; also available: per-run cost telemetry, portfolio lens, batched Reddit sentiment if creds). Create the session worktree + branch (feat/session-19-<slug>) once the slice is known, confirm the task list with the user, batch the work, validate (pytest expect 75+, full dry-run 63 ok, rawSql with SET collation_connection = utf8mb4_unicode_ci if dashboards touched, migrations only with user sign-off), and close out per the ritual: update PLAN.md, write handoffs/HANDOFF_19.md with the complete next prompt + a detailed TODO an older model can follow + fresh suggestions, push without merging, and print the full next-session prompt in the chat.
