# Handoff 24 — 2026-07-12 (session 24: session-23 merge + deep-dive uid + per-run cost telemetry)

Continues [HANDOFF_23.md](HANDOFF_23.md). Evergreen orientation in [HANDOFF.md](HANDOFF.md); rolling status/roadmap in [PLAN.md](../PLAN.md); structural map in [PROJECT_SUMMARY.md](../PROJECT_SUMMARY.md); design spec in [SPEC.md](../SPEC.md).

## What session 24 did

On branch **`feat/session-24-cost-telemetry`** (off `main` @ `1b7ca14`), worktree `.claude/worktrees/session-24-cost-telemetry`. Pushed, **not merged**.

Session start: **the handoff prompt and the repo agreed** — third clean session in a row (no merged-but-unpushed drift). Session ran on **Sunday 2026-07-12** (same day as s22/s23), so every calendar-gated verification (Mon/Wed cadence runs, Friday retro + patterns) **carries forward untouched again**.

1. **Merged session 23** (`feat/session-23-horizon-180` ff → **`main` @ `36ad85c`, pushed Sun 07-12**) after the user imported all three touched dashboards (track-record + predictions trend charts with the orange `180d hit %` series; deep-dive 6m columns) and approved. 180d grading accumulates from Monday's run.
2. **Closed the s21 leftover on its third chase**: the user provided the real deep-dive uid — **`ma2wqvp`** — committed into `predictions_dashboard.json` (**`main` @ `1b7ca14`**): the symbol data-link URL plus both doc mentions (panel-1 description + how-to-read) now ship pre-wired. Caveat: the copy the user imported that morning still had the placeholder — they should **re-import main's copy** or do the one-time link edit in Grafana.
3. **The chosen slice — per-run cost telemetry in the DB** (user picked the recommended option; the ETF-WATCH intervention was deliberately left for after Friday's third confirmation):
   - **Migration 008** ([migrations/008_create_run_metrics.sql](../migrations/008_create_run_metrics.sql), sign-off given, **applied to the live DB**, verified created with 0 rows): `run_metrics` — append-only, one row per completed run: `run_at`, `model_used`, `calls`, plain + batched token counts (batched separate — the Batches API bills 50%), cache counters (expected 0 since s08), `estimated_cost_usd DECIMAL(10,6)`, `tickers_ok`/`tickers_failed`, `INDEX idx_run_at`.
   - **`ClaudeClient.usage_snapshot()`** ([src/analysis/claude_client.py](../src/analysis/claude_client.py)): public copy of the run's accumulated `_usage` counters (there was no public getter).
   - **`write_run_metrics`** ([src/persistence/writers.py](../src/persistence/writers.py)): standard writer shape — dry-run short-circuit (`[dry-run] Would insert run metrics ...`), `_utcnow()`, `MODEL` as `model_used`, cost rounded to 6 decimals. **Dry-runs never write, so every row is a real run.**
   - **main step 12** ([src/main.py](../src/main.py)): persists usage + cost + ok/failed at run end — placed **after step 11 and inside the `try`** (all 65+ calls counted; the log-only `claude.log_usage()` after `conn.close()` is unchanged), wrapped in its own try/except so a failure never kills the run.
   - **Digest panel-17 "Coste por corrida (USD)"** ([grafana/daily_digest_dashboard.json](../grafana/daily_digest_dashboard.json)): green bars of `estimated_cost_usd` per run, currencyUSD unit, 4 decimals, id 17 at y=122 (below panel-16). Time-picker-scoped via `$__timeFilter(run_at)`. No backfill exists — historical costs live only in workflow logs; the series starts with the first production run after the merge.

## Validation evidence

- **pytest: 108 passed** (100 + 8 new in [tests/test_run_metrics.py](../tests/test_run_metrics.py): snapshot starts zeroed with all 7 keys / is a copy not the live dict / reflects recorded usage incl. the batch split; writer row shape (all 12 params checked) + dry-run touches nothing; main wiring — written once at run end with ok=1/failed=0, `dry_run=True` forwarded, a writer exception never propagates out of `main()`). The 7 existing main-harness `ClaudeClient` stubs across the suite gained `usage_snapshot`/`estimated_cost_usd`, and each harness stubs `write_run_metrics`.
- **Full real-API dry-run: 63 ok / 0 failed at $0.0957** (in line with the ~$0.097 baseline). The new step-12 line fired exactly once, after all 65 calls: `[dry-run] Would insert run metrics (65 calls, $0.0957, ok=63 failed=0)`, followed by the unchanged `Run complete` + `Claude usage` lines. 12 flips vs the stored 07-10 run, repeating the known consistent cohort (NBIS SELL→HOLD, AMPX HOLD→SELL, SMCI/CLS AVOID→WATCH, MU AVOID→BUY, VWRL.AS WATCH→BUY…) — dry-run comparison across a 2-day gap, judge the trend not the count.
- **Panel-17 rawSql green live** (collation init_command; `$__timeFilter` → literal BETWEEN): 0 rows on the empty table — correct. Dashboard JSON parses; elements/layout consistent (panels 1–17, no grid overlap); the diff is purely additive (+129 lines — surgical edits, no `json.dump` round-trip).
- **Migration verified applied**: `SHOW CREATE TABLE run_metrics` matches the file; `SELECT COUNT(*)` = 0.
- **Learned the hard way**: keep `;` out of migration *comments* — a naive split-on-`;` applier choked twice (header comment had `log_usage());`, column comments had `08; kept`) before the file was cleaned. The committed 008 has no semicolons outside the DDL.

## Invariants (don't break)

All of HANDOFF_23's invariants stand (Friday stays in the cron — retro + mining key on it; `prediction_patterns` append-only, newest row = truth; the miner must always receive the previous set; no `minimum`/`maximum` on numbers in structured-output schemas; `recommendations.fundamentals` NULL when unfetched; digest panels 15/16 not time-picker-scoped; flips annotation `filter.ids: [5]`; flip counts read higher under Mon/Wed/Fri gaps; never write to stock-snapshots tables; `--dry-run` = no writes, real API; Spanish prompts; no persisted placeholders; 30d headline; `HORIZONS` derives from `HORIZON_BANDS`; band monotonicity test-enforced; `get_latest_actions` before 6a; ETF/fundamentals fetches mutually exclusive by quote_type; `dividendYield` percent units; schema-v2 rules — no `timeSettings.weekStart/nowDelay/quickRanges`, no `vizConfig.spec.version`; grafana JSONs mixed unicode — surgical edits only; batch custom_ids `[A-Za-z0-9_-]`; worktree-per-session; validate with the main checkout's `.venv`). New this session:

- **`run_metrics` is append-only and dry-runs never write** — every row is a real run; no upserts, no placeholder rows, no backfill.
- **Step 12 must stay last** (after retro/patterns) so the persisted usage covers every call of the run; and it must stay fail-soft (its own try/except) — cost telemetry must never kill a run.
- **The `DEEP_DIVE_UID` placeholder is gone** — `predictions_dashboard.json` carries the real uid `ma2wqvp`. Don't reintroduce the placeholder; if the user's Grafana ever re-assigns a uid, update the JSON.
- **`ClaudeClient._usage` stays private** — external readers go through `usage_snapshot()`.
- **Any new main-harness test must stub `usage_snapshot`/`estimated_cost_usd`/`write_run_metrics`** (all 7 existing harnesses already do).
- **No `;` inside migration comments** (naive statement splitters break).

## State of play / caveats

- **`main` = `1b7ca14`** (s23 merged @ `36ad85c` + the uid commit, both pushed Sun 07-12). Session 24 on `feat/session-24-cost-telemetry`, pushed, **awaiting merge**.
- **Merge gate for s24 is light**: migration 008 is already applied, so the only gate is the user importing `daily_digest_dashboard.json` from the s24 branch and confirming panel-17 renders (**legitimately empty** until the first post-merge production run) + approval, then ff-merge + push. **Merging before Mon 07-13 10:00 UTC starts the cost series with Monday's run**; missing that just loses data points (no breakage — an unmerged branch means runs simply don't persist cost rows).
- **Predictions dashboard**: uid committed, but the user's imported copy may still carry the placeholder — remind them to re-import main's copy (or edit the link once in Grafana). Then symbol click-through works.
- **Mon 2026-07-13 run** = first under the Mon/Wed/Fri cadence with s16+s17+s18+s20 live: 63 ok, cost ≈ $0.097, flips vs 11 on 07-10 (2-day gaps read higher), fundamentals citations in EQUITY reasonings; confirm **no Tue/Thu runs**; the evaluate step should write the ~165 pending 30d outcomes seen on 07-12; **if s24 merged in time, the first `run_metrics` row appears** (check panel-17 / `SELECT * FROM run_metrics`).
- **Fri 2026-07-17 run** = first production retro row AND first production patterns row. `prediction_patterns` seed row id=1 (8 NEW, 2026-07-12) exists: Friday's row should be id>1 with **CONFIRMED/REVISED statuses — all-NEW means `get_latest_patterns` isn't reaching the prompt; investigate**. Note whether **WATCH×ETF holds ~14%** (already replicated twice, conf 0.91): a third sighting confirms the ETF-WATCH intervention slice.
- Carried: ETF bucket 37% baseline (track-record panel-6); 252 pre-price_checks candidates ungradeable; price_checks 06-30→07-08 gap permanent; 90d/365d/180d series fill ~2026-08-15 / 2027-05-17 / 2026-11-13; deep-dive price history starts 2026-06-12; price_checks 3×/week (exit prices up to 2 days off maturity); Reddit dark (`grep -c '^REDDIT_' .env` = 0, 2026-07-12).
- **Local runs**: `env -u ANTHROPIC_API_KEY` (empty shell var shadows `.env`); worktrees have no `.venv` — use `/home/guillo/Git/stock-recommendations/.venv/bin/python`; ad-hoc DB scripts need `load_dotenv('/home/guillo/Git/stock-recommendations/.env')`, `DB_HOST/DB_PORT/DB_USER/DB_PASS/DB_NAME`, and init_command `SET collation_connection = utf8mb4_unicode_ci` for dashboard rawSql. Substitute `$ticker` with a quoted CSV and `$__timeFilter(...)` with a literal BETWEEN.
- **User timezone: Europe/Madrid.**

## Detailed TODO for session 25 (step-by-step; follow in order)

**Step 0 — Orient.** Read this file and [PLAN.md](../PLAN.md). From the main checkout: `git pull`, `git log --oneline -3 --decorate`, compare against the prompt — **if they disagree, the repo wins; say so** (s19–s21 found drift; s22–s24 did not — check anyway). `git worktree list`; `git status --short` in the newest worktree for interrupted work.

**Step 1 — Merge gate for session 24.** Ask the user (AskUserQuestion) to import `grafana/daily_digest_dashboard.json` from the s24 branch/worktree and confirm panel-17 "Coste por corrida (USD)" appears at the bottom (empty until the first post-merge production run — that's correct; if a run has already happened post-merge it shows green bars ≈ $0.10). On approval: `git checkout main && git merge --ff-only feat/session-24-cost-telemetry && git push`. Migration 008 is already applied — merging is low-risk. Also remind: re-import main's `predictions_dashboard.json` (uid `ma2wqvp` pre-wired) if their imported copy still has the placeholder, and confirm symbol click-through lands on the deep-dive filtered to that ticker.

**Step 2 — Verify the week's production runs.** `gh run list --workflow=run_recommendations.yml --limit 6`. For Mon 07-13 + Wed 07-15 (whichever have fired): 63 ok / 0 failed; cost ≈ $0.097; flips line (2-day-gap caveat); spot-check 2–3 EQUITY reasonings for P/E/margin/growth citations; **confirm no Tue/Thu runs**. Monday's evaluate step should have written ~165 pending 30d outcomes. **If s24 was merged before a run: `SELECT * FROM run_metrics ORDER BY id` — one row per production run since, cost ≈ the log line, panel-17 shows bars.** Record in PLAN.

**Step 3 — First production Friday (on/after 2026-07-17).** Verify: (a) **retro** — log lines + `SELECT week_start, LEFT(retrospective,200) FROM weekly_retrospectives` + digest panel-12; (b) **patterns** — "Mining prediction patterns..." + "Inserted prediction patterns row" in the log, `SELECT id, generated_at, JSON_LENGTH(patterns) FROM prediction_patterns ORDER BY id DESC LIMIT 2` (a new row above the 07-12 seed), panels 15/16 refreshed, and **statuses NOT all-NEW** (all-NEW = `get_latest_patterns` broken — investigate). Note whether WATCH×ETF holds ~14% (third sighting → ETF-WATCH slice confirmed); (c) if s24 merged, Friday's `run_metrics` row should show ~67 calls (65 + retro + patterns) at a slightly higher cost.

**Step 4 — Check Reddit creds** (`grep -c '^REDDIT_' .env`). If >0: GitHub secrets, one real cycle, `reddit_mentions`/`trending_tickers`/panel-14, batched sentiment becomes buildable.

**Step 5 — Pick the next slice with the user** (AskUserQuestion). Suggested order:
1. **ETF-WATCH prompt intervention** (if Friday confirmed the ~14% bucket — it's replicated twice already): a Spanish caution in the per-ticker prompt for WATCH candidates whose quote_type is ETF (e.g. inside `_etf_block`: the system's WATCH calls on ETFs have historically failed — prefer HOLD/BUY/AVOID unless the reasoning names a catalyst). Prompt-only, cheap; validate with a full dry-run and compare the WATCH×ETF count vs the ~27-ETF baseline.
2. **Patterns→prompt feedback loop** (design-first): inject top CONFIRMED patterns into the per-ticker prompt, gated on status+confidence+sample size. After Friday there will be a production CONFIRMED set to build against. Needs a design conversation before code.
3. **Pattern-evolution panel** (small): stacked bars of pattern-count by status per `generated_at` week — becomes meaningful once 2–3 Friday rows exist.
4. **Benchmark-relative + dividend-adjusted grading** (~2026-08-15, needs decisions + re-grade sign-off), portfolio lens dashboard, batched Reddit sentiment (if creds).

**Step 6 — Validate the standard way.** `env -u ANTHROPIC_API_KEY /home/guillo/Git/stock-recommendations/.venv/bin/python -m pytest tests/ -q` (expect **108+**); full dry-run (63 ok / 0 failed; add `--force-patterns`/`--force-retro` if those paths were touched) **only if pipeline code changed**; re-extract touched dashboard rawSql and run live (collation init_command; `$ticker` → quoted CSV; `$__timeFilter` → BETWEEN); migrations only with sign-off (and no `;` in their comments). No `minimum`/`maximum` in structured-output schemas. Don't round-trip grafana JSONs through json.dump. New main-harness tests must stub `usage_snapshot`/`estimated_cost_usd`/`write_run_metrics`.

**Step 7 — Close out per the ritual.** Update PLAN.md; write `handoffs/HANDOFF_25.md` (complete copy-pasteable next prompt + detailed TODO an older model can follow + fresh suggestions); commit; push the branch (no merge unless asked); print the full next prompt in chat.

## Fresh suggestions (beyond the committed backlog)

- **Weekly cost rollup on panel-17** once ~2 weeks of rows exist: a second query summing cost per ISO week would show the bill trend the cadence decision was made for (~$0.29/week expected).
- **run_metrics × outcome join** (later, needs months of rows): cost-per-correct-call — the ultimate "is this worth it" metric.
- **Panel the pattern evolution** (carried): stacked bars by status per week over `prediction_patterns.generated_at` once 3–4 Friday rows exist.
- **Retire the daily-era flip baselines** once two Mon/Wed/Fri weeks exist (recompute panel-13 expectations; carried).
- **Retry-once on transient yfinance timeouts** (carried; cheap, stub-tested).
- **A 180d how-to-read sweep in ~Nov 2026** (carried): when the first 6m verdicts land, re-check the five-series trend legends.

## Prompt for the next session (copy-paste exactly)

> Read handoffs/HANDOFF_24.md and PLAN.md before doing anything — HANDOFF_24 has the detailed step-by-step TODO for session 25; follow it in order, and first cross-check this prompt's claims against git log (if they disagree, the repo wins — say so; s19–s21 found drift, s22–s24 didn't but check anyway). Context: main is at 1b7ca14 (session 23's real 180d horizon merged @ 36ad85c + the deep-dive uid ma2wqvp committed into predictions_dashboard.json — the DEEP_DIVE_UID placeholder is gone; both pushed Sun 2026-07-12). Session 24 (Sunday 2026-07-12, branch feat/session-24-cost-telemetry, pushed NOT merged) delivered per-run cost telemetry in the DB: migration 008 run_metrics is ALREADY APPLIED to the live DB with sign-off (append-only, one row per completed run — batched tokens separate at the 50% rate, estimated_cost_usd DECIMAL(10,6), tickers_ok/failed; dry-runs never write); ClaudeClient.usage_snapshot() (new public accessor — _usage stays private), write_run_metrics (standard writer, dry-run short-circuit), main step 12 (last, inside the try, fail-soft — a metrics failure never kills the run), digest panel-17 "Coste por corrida (USD)" (green bars, currencyUSD, empty until the first post-merge production run — no backfill exists). Validation: pytest 108 passed (8 new in tests/test_run_metrics.py; the 7 main-harness ClaudeClient stubs gained usage_snapshot/estimated_cost_usd + a write_run_metrics stub — any new harness needs the same); full real-API dry-run 63 ok / 0 failed at $0.0957 with the new line firing once after all 65 calls ("[dry-run] Would insert run metrics (65 calls, $0.0957, ok=63 failed=0)") and 12 flips repeating the known consistent cohort; panel-17 rawSql green live (0 rows = correct-empty); migration verified applied (SHOW CREATE TABLE matches, 0 rows). Steps: (1) merge gate — user imports daily_digest_dashboard.json from the s24 branch (panel-17 at the bottom, legitimately empty until the first post-merge run), then ff-merge feat/session-24-cost-telemetry to main and push (before Mon 10:00 UTC starts the cost series with Monday's run; no hard deadline, only lost data points); remind the user to re-import main's predictions_dashboard.json (uid pre-wired) if their copy still has the placeholder, and confirm symbol click-through; (2) verify Mon 07-13 + Wed 07-15 production runs (63 ok, ≈$0.097, flips read higher across 2-day gaps — judge the trend; fundamentals citations in EQUITY reasonings; NO Tue/Thu runs; Monday's evaluate step should write ~165 pending 30d outcomes; if s24 merged pre-run, run_metrics rows + panel-17 bars appear); (3) on/after Fri 07-17 verify the first production retro (log + weekly_retrospectives row + panel-12) AND the first production patterns row (log lines + new prediction_patterns row above the 07-12 seed id=1 + panels 15/16 refreshed; statuses NOT all-NEW — all-NEW means get_latest_patterns isn't reaching the prompt; note if WATCH×ETF holds ~14% — a third sighting confirms the ETF-WATCH intervention slice; Friday's run_metrics row ≈ 67 calls); (4) check Reddit creds (grep -c '^REDDIT_' .env — 0 as of 07-12). Then pick the next slice with the user from HANDOFF_24 step 5 (suggested: the ETF-WATCH prompt intervention if Friday confirmed; or the patterns→prompt feedback loop, design-first — Friday's CONFIRMED set makes it buildable; or the pattern-evolution panel once rows accumulate). Create the session worktree + branch (feat/session-25-<slice>), confirm the task list with the user, batch the work, validate (pytest expect 108+ using the main checkout's .venv — worktrees have none; full dry-run 63 ok only if pipeline code changed, add --force-patterns/--force-retro if those paths were touched; rawSql live with SET collation_connection = utf8mb4_unicode_ci, $ticker → quoted CSV, $__timeFilter → literal BETWEEN; migrations only with user sign-off and no semicolons in migration comments; structured-output schemas must NOT use minimum/maximum on numbers; grafana JSONs have mixed unicode conventions — surgical text edits only, never a full json.dump round-trip), and close out per the ritual: update PLAN.md, write handoffs/HANDOFF_25.md with the complete next prompt + a detailed TODO an older model can follow + fresh suggestions, push without merging, and print the full next-session prompt in the chat.
