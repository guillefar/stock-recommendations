# Handoff 23 — 2026-07-12 (session 23: session-22 merge + patterns seed + real 180d horizon)

Continues [HANDOFF_22.md](HANDOFF_22.md). Evergreen orientation in [HANDOFF.md](HANDOFF.md); rolling status/roadmap in [PLAN.md](../PLAN.md); structural map in [PROJECT_SUMMARY.md](../PROJECT_SUMMARY.md); design spec in [SPEC.md](../SPEC.md).

## What session 23 did

On branch **`feat/session-23-horizon-180`** (off `main` @ `2226665`), worktree `.claude/worktrees/session-23-horizon-180`. Pushed, **not merged**.

Session start: **the handoff prompt and the repo agreed** — second clean session in a row (no merged-but-unpushed drift). Session ran on **Sunday 2026-07-12** (same day as s22), so every calendar-gated verification (Mon/Wed cadence runs, Friday retro + patterns) **carries forward untouched**.

1. **Merged session 22** (`feat/session-22-patterns` ff → **`main` @ `2226665`, pushed Sun 07-12**) after the user imported both touched dashboards (digest panels 15/16; deep-dive 90d/1y columns) and approved. Landed **well before Fri 07-17 10:00 UTC — Friday's production run WILL mine patterns.**
2. **Seeded `prediction_patterns`** (user opted in, $0.0163): one real ad-hoc mining run using the exact main-step-11 path (`get_outcome_features` → 1,134 rows → `summarize_features` → `get_latest_patterns` (None, cold start) → `run_pattern_analysis` → `write_prediction_patterns`). Result: **row id=1, 8 patterns, all NEW**, and the **WATCH×ETF ~14% finding replicated independently** ("WATCH en ETFs es un lastre sistemático", confidence 0.91; HOLD×ETF strength and the 0.60–0.79 confidence sweet spot also reappeared). Digest panels 15/16 verified rendering populated live (8 pattern rows + narrative). **Friday's mining now receives this set as "previous" — expect CONFIRMED/REVISED statuses, not all-NEW.**
3. **The user's pinned ask — a real 180d (6m) grading horizon** (bands **±10/21/25 signed off**: √time from the 90d bands ±7/15/20, hold_loss trimmed to 25% to stay monotonic under 365d's 30%):
   - [src/evaluate_outcomes.py](../src/evaluate_outcomes.py): `HORIZON_BANDS[180] = Bands(neutral=0.10, watch_move=0.21, hold_loss=0.25)`. `HORIZONS` derives from the dict, so the evaluator grades the fifth horizon with **no other pipeline change and no re-grade** — 180d rows accumulate as recs mature. Earliest rec 2026-05-17 → **first 6m verdicts ≈ 2026-11-13**.
   - **Trend charts** ([grafana/track_record_dashboard.json](../grafana/track_record_dashboard.json), [grafana/predictions_dashboard.json](../grafana/predictions_dashboard.json)): the shared weekly hit-rate rawSql gains a `180d hit %` column (between 90d and 365d) + an orange `byName` color override (90d=blue, 1y=purple); descriptions updated; the track-record how-to-read's stale "first 90d ≈ Oct 2026" fixed to Aug 2026 (same fix s22 made on the deep-dive).
   - **Deep-dive Call history** ([grafana/ticker_deep_dive_dashboard.json](../grafana/ticker_deep_dive_dashboard.json)): `6m verdict`/`6m return %` columns via an `o180` LEFT JOIN, verdict color-mapping + width overrides copied from the 90d/1y pairs; panel description + panel-4 how-to-read updated (first 6m ≈ mid-Nov 2026; "a 6-month BUY must beat +10%"). The **hit-rate-by-horizon table needed nothing** — it's `GROUP BY o.horizon_days`, so 180d rows appear automatically when they mature.
   - **Docs**: PROJECT_SUMMARY outcomes-table line (7d/30d/90d/180d/365d), README deep-dive blurb.

## Validation evidence

- **pytest: 100 passed** (98 + 2 new in [tests/test_outcomes.py](../tests/test_outcomes.py): `test_180d_bands` — BUY/WATCH/HOLD edges at ±10/21/25 — and `test_bands_monotonic_across_horizons` — every threshold widens (hold_loss non-strict) as the horizon grows, so a verdict never gets easier at a longer horizon; `test_every_horizon_has_bands` updated to `(7, 30, 90, 180, 365)`).
- **Live evaluator dry-run** (`python -m src.evaluate_outcomes --dry-run` from the worktree with main's `.venv`): logs **"Horizon 180d: graded 0 of 0 matured candidates"** — the clean no-op expected today — while 30d graded 165 pending candidates normally (dry-run wrote nothing; Monday's production run will write those).
- **All touched rawSql green live** (collation init_command): both trend queries return the `180d hit %` column (all NULL — no 180d rows exist); deep-dive call history under multi `'AAPL','MU','XESC.DE'` (108 rows, 54 with 30d verdicts), single `'AAPL'` (36), unknown (0) — `6m` columns NULL everywhere as expected. All three JSONs parse.
- **No `src/main.py`/prompt/collector changes → no recommendation dry-run** (evaluator + dashboards only; same rule as sessions 10/19/21).
- **Seed run evidence**: `SELECT id, generated_at, horizon_days, JSON_LENGTH(patterns) FROM prediction_patterns` → `(1, 2026-07-12 14:18:43, 30, 8)`; panel-15 rawSql returns 8 rows (top: WATCH×ETF, NEW, 91%), panel-16 returns the narrative. Claude usage: 1 call, $0.0163.

## Invariants (don't break)

All of HANDOFF_22's invariants stand (Friday stays in the cron — retro + mining key on it; `prediction_patterns` append-only, newest row = truth; the miner must always receive the previous set; no `minimum`/`maximum` on numbers in structured-output schemas; `recommendations.fundamentals` NULL when unfetched; digest panels 15/16 not time-picker-scoped; flips annotation `filter.ids: [5]`; `DEEP_DIVE_UID` placeholder is not a bug; flip counts read higher under Mon/Wed/Fri gaps; never write to stock-snapshots tables; `--dry-run` = no writes, real API; Spanish prompts; no persisted placeholders; 30d headline; `get_latest_actions` before 6a; ETF/fundamentals fetches mutually exclusive by quote_type; `dividendYield` percent units; schema-v2 rules — no `timeSettings.weekStart/nowDelay/quickRanges`, no `vizConfig.spec.version`; batch custom_ids `[A-Za-z0-9_-]`; worktree-per-session; validate with the main checkout's `.venv`). New this session:

- **`HORIZONS` derives from `HORIZON_BANDS`** — never hard-code the horizon tuple anywhere else; adding a horizon is a one-dict-entry change + dashboard columns.
- **Band monotonicity is now test-enforced** (`test_bands_monotonic_across_horizons`): any future band edit must keep neutral/watch_move strictly increasing and hold_loss non-decreasing across horizons.
- **The grafana JSONs have mixed unicode conventions** (track-record/deep-dive store escaped `\u2248`; predictions stores literal `—`): never round-trip a whole file through `json.dump` — make surgical text edits, or the diff explodes.
- **30d stays the headline everywhere**; 180d is a maturing series like 90d/365d, not a new default.

## State of play / caveats

- **`main` = `2226665`** (s22 merged + pushed Sun 07-12 — Friday 07-17 mines patterns in production). Session 23 on `feat/session-23-horizon-180`, pushed, **awaiting merge**.
- **Merge gate for s23 is light**: no migrations, no prompts — the user re-imports the three touched dashboards from the s23 branch (track-record + predictions trend charts: new orange `180d hit %` series, legitimately absent-from-legend/empty until ~2026-11; deep-dive Call history: `6m verdict`/`6m return %` columns, legitimately empty) and approves. **No calendar deadline**: 180d grading simply starts when the merge lands, and nothing matures before 2026-11-13 anyway — but merging before Monday costs nothing and starts the clock.
- **Predictions dashboard s21 leftover carries AGAIN**: not yet imported; `DEEP_DIVE_UID` still a placeholder. The s23 branch touches that same JSON (trend series), so have the user import the **s23 copy** to clear both at once; commit the real uid if provided.
- **prediction_patterns is seeded** (row id=1, 8 NEW patterns, 2026-07-12): Friday's row should show id>1 with CONFIRMED/REVISED statuses. If Friday's set is somehow all-NEW, `get_latest_patterns` isn't reaching the prompt — investigate.
- **Mon 2026-07-13 run** = first under the Mon/Wed/Fri cadence with s16+s17+s18+s20 live: 63 ok, cost ≈ $0.097, flips vs 11 on 07-10 (2-day gaps read higher — judge the trend), fundamentals citations in EQUITY reasonings; confirm **no Tue/Thu runs**. Note: the 07-12 evaluator dry-run saw ~165 matured-ungraded 30d candidates — Monday's evaluate step should write a batch that size.
- **Fri 2026-07-17 run** = first production retro row AND first production patterns row (see step 3 below).
- **WATCH×ETF ~14% has now shown up twice independently** (s22 ad-hoc + s23 seed, conf 0.91). If Friday's production mining confirms it again, the ETF-WATCH prompt intervention is ready to build.
- Carried: ETF bucket 37% baseline (track-record panel-6); 252 pre-price_checks candidates ungradeable; price_checks 06-30→07-08 gap permanent; 90d/365d series fill ~2026-08-15 / 2027-05-17 (180d ~2026-11-13); deep-dive price history starts 2026-06-12; price_checks 3×/week (exit prices up to 2 days off maturity); Reddit dark (`grep -c '^REDDIT_' .env` = 0, 2026-07-12).
- **Local runs**: `env -u ANTHROPIC_API_KEY` (empty shell var shadows `.env`); worktrees have no `.venv` — use `/home/guillo/Git/stock-recommendations/.venv/bin/python`; ad-hoc DB scripts need `load_dotenv('/home/guillo/Git/stock-recommendations/.env')`, `DB_HOST/DB_PORT/DB_USER/DB_PASS/DB_NAME`, and init_command `SET collation_connection = utf8mb4_unicode_ci` for dashboard rawSql. Substitute `$ticker` with a quoted CSV and `$__timeFilter(...)` with a literal BETWEEN.
- **User timezone: Europe/Madrid.**

## Detailed TODO for session 24 (step-by-step; follow in order)

**Step 0 — Orient.** Read this file and [PLAN.md](../PLAN.md). From the main checkout: `git pull`, `git log --oneline -3 --decorate`, compare against the prompt — **if they disagree, the repo wins; say so** (s19–s21 found drift; s22/s23 did not — check anyway). `git worktree list`; `git status --short` in the newest worktree for interrupted work.

**Step 1 — Merge gate for session 23.** Ask the user (AskUserQuestion) to import from the s23 branch/worktree: (a) `grafana/track_record_dashboard.json` + `grafana/predictions_dashboard.json` — the weekly trend chart carries an orange `180d hit %` series (no data until ~Nov 2026 is correct); (b) `grafana/ticker_deep_dive_dashboard.json` — Call history shows `6m verdict`/`6m return %` (empty is correct). On approval: `git checkout main && git merge --ff-only feat/session-23-horizon-180 && git push`. No calendar deadline, but merging starts 180d accumulation. **Also chase the s21 leftover a third time**: predictions_dashboard import + `DEEP_DIVE_UID` replacement (segment after `/d/` in the deep-dive URL) — importing the s23 copy clears both; commit the uid if provided.

**Step 2 — Verify the week's production runs.** `gh run list --workflow=run_recommendations.yml --limit 6`. Mon 07-13 + Wed 07-15 (whichever have fired): 63 ok / 0 failed; cost ≈ $0.097; flips line (2-day-gap caveat — judge the trend, not daily-era absolutes); spot-check 2–3 EQUITY reasonings for P/E/margin/growth citations; ETFs cite exposure/costs. **Confirm no Tue/Thu runs fired.** Monday's evaluate step should also have written a large 30d outcomes batch (~165 were pending on 07-12). Record in PLAN.

**Step 3 — First production Friday (on/after 2026-07-17).** Verify: (a) **retro** — log lines + `SELECT week_start, LEFT(retrospective,200) FROM weekly_retrospectives` + digest panel-12; (b) **patterns** — "Mining prediction patterns..." + "Inserted prediction patterns row" in the log, `SELECT id, generated_at, JSON_LENGTH(patterns) FROM prediction_patterns ORDER BY id DESC LIMIT 2` (expect a new row above the 07-12 seed), digest panels 15/16 show the refreshed set, and **statuses should NOT be all-NEW** (the seed row exists — CONFIRMED/REVISED prove the refinement loop works; all-NEW means `get_latest_patterns` isn't reaching the prompt). Note whether WATCH×ETF holds ~14% (third sighting → the ETF-WATCH intervention slice is confirmed).

**Step 4 — Check Reddit creds** (`grep -c '^REDDIT_' .env`). If >0: GitHub secrets, one real cycle, `reddit_mentions`/`trending_tickers`/panel-14, batched sentiment becomes buildable.

**Step 5 — Pick the next slice with the user** (AskUserQuestion). Suggested order:
1. **ETF-WATCH prompt intervention** (if Friday confirmed the ~14% bucket — likely, it's replicated twice): a Spanish caution in the per-ticker prompt for WATCH candidates whose quote_type is ETF (e.g. inside `_etf_block`: the system's WATCH calls on ETFs have historically failed — prefer HOLD/BUY/AVOID unless the reasoning names a catalyst). Prompt-only, cheap; validate with a full dry-run and compare the WATCH×ETF count vs the ~27-ETF baseline. Fold in the digest panel-13 flip-baseline note (2-day gaps).
2. **Per-run cost telemetry in the DB** (migration — needs sign-off): persist each run's token/cost line (`runs` table or `daily_summaries` columns); a cost-trend panel then proves the −40% cadence saving with data.
3. **Patterns→prompt feedback loop** (the natural evolution of the user's pattern ask): inject top CONFIRMED patterns into the per-ticker prompt. Gate on status+confidence+sample size; measure before/after. Bigger slice — needs a design conversation first.
4. **Benchmark-relative + dividend-adjusted grading** (~2026-08-15, needs decisions + re-grade sign-off), portfolio lens dashboard, batched Reddit sentiment (if creds).

**Step 6 — Validate the standard way.** `env -u ANTHROPIC_API_KEY /home/guillo/Git/stock-recommendations/.venv/bin/python -m pytest tests/ -q` (expect **100+**); full dry-run (63 ok / 0 failed; add `--force-patterns`/`--force-retro` if those paths were touched) **only if pipeline code changed**; re-extract touched dashboard rawSql and run live (collation init_command; `$ticker` → quoted CSV; `$__timeFilter` → BETWEEN); migrations only with sign-off. No `minimum`/`maximum` in structured-output schemas. Don't round-trip grafana JSONs through json.dump (mixed unicode conventions — surgical edits only).

**Step 7 — Close out per the ritual.** Update PLAN.md; write `handoffs/HANDOFF_24.md` (complete copy-pasteable next prompt + detailed TODO an older model can follow + fresh suggestions); commit; push the branch (no merge unless asked); print the full next prompt in chat.

## Fresh suggestions (beyond the committed backlog)

- **Panel the pattern evolution**: `prediction_patterns` is append-only audit trail — a digest panel showing pattern-count by status per week (stacked bars over `generated_at`) would make the learning loop visible once 3–4 Friday rows exist.
- **Retire the daily-era flip baselines** once two Mon/Wed/Fri weeks exist (recompute panel-13 expectations; carried).
- **Commit the real deep-dive uid once known** (carried from s21, chased twice) — kills the placeholder caveat in three doc spots.
- **Retry-once on transient yfinance timeouts** (carried; cheap, stub-tested).
- **A 180d how-to-read sweep in ~Nov 2026**: when the first 6m verdicts land, re-check that trend legends/orders render sensibly with five series.

## Prompt for the next session (copy-paste exactly)

> Read handoffs/HANDOFF_23.md and PLAN.md before doing anything — HANDOFF_23 has the detailed step-by-step TODO for session 24; follow it in order, and first cross-check this prompt's claims against git log (if they disagree, the repo wins — say so; s19–s21 found drift, s22/s23 didn't but check anyway). Context: main is at 2226665 (session 22 patterns+fundamentals merged + pushed Sun 2026-07-12 — Friday 07-17 10:00 UTC WILL mine patterns in production; prediction_patterns was seeded the same day with a real ad-hoc mining run: row id=1, 8 all-NEW patterns incl. WATCH×ETF ~14% conf 0.91, so Friday's row should show CONFIRMED/REVISED statuses — all-NEW would mean get_latest_patterns isn't reaching the prompt). Session 23 (Sunday 2026-07-12, branch feat/session-23-horizon-180, pushed NOT merged) delivered the user's pinned real 180d (6m) grading horizon: HORIZON_BANDS[180] = ±10/21/25 (user-approved, √time from 90d, hold_loss trimmed for monotonicity under 365d) in src/evaluate_outcomes.py — HORIZONS derives automatically, no re-grade, first 6m verdicts ≈ 2026-11-13; trend charts in track_record + predictions dashboards gained an orange `180d hit %` series; deep-dive Call history gained 6m verdict/return columns (o180 join); track-record's stale "first 90d ≈ Oct 2026" fixed to Aug 2026. Validation: pytest 100 passed (2 new: 180d band edges + cross-horizon monotonicity, now test-enforced); live evaluate_outcomes --dry-run logged "Horizon 180d: graded 0 of 0 matured candidates" (clean no-op; ~165 matured 30d candidates were pending — Monday's run writes them); all touched rawSql green live; no pipeline-prompt changes so no recommendation dry-run. Steps: (1) merge gate — user imports the three touched dashboards from the s23 branch (track_record + predictions: orange 180d trend series, empty until ~Nov 2026 is correct; deep-dive: 6m columns, empty is correct), then ff-merge feat/session-23-horizon-180 to main and push (no calendar deadline); chase the s21 leftover a THIRD time: predictions_dashboard.json import + DEEP_DIVE_UID replacement — the s23 copy clears both at once, commit the uid if provided; (2) verify Mon 07-13 + Wed 07-15 production runs (63 ok, ≈$0.097, flips read higher across 2-day gaps — judge the trend; fundamentals citations in EQUITY reasonings; NO Tue/Thu runs; Monday's evaluate step should write ~165 pending 30d outcomes); (3) on/after Fri 07-17 verify the first production retro (log + weekly_retrospectives row + panel-12) AND the first production patterns row (log lines + new prediction_patterns row above the seed + panels 15/16 refreshed; statuses NOT all-NEW; note if WATCH×ETF holds ~14% — a third sighting confirms the ETF-WATCH intervention slice); (4) check Reddit creds (grep -c '^REDDIT_' .env — 0 as of 07-12). Then pick the next slice with the user from HANDOFF_23 step 5 (suggested: the ETF-WATCH prompt intervention if Friday confirms; or per-run cost telemetry with migration sign-off; or the patterns→prompt feedback loop, design-first). Create the session worktree + branch (feat/session-24-<slice>), confirm the task list with the user, batch the work, validate (pytest expect 100+ using the main checkout's .venv — worktrees have none; full dry-run 63 ok only if pipeline code changed, add --force-patterns/--force-retro if those paths were touched; rawSql live with SET collation_connection = utf8mb4_unicode_ci, $ticker → quoted CSV, $__timeFilter → literal BETWEEN; migrations only with user sign-off; structured-output schemas must NOT use minimum/maximum on numbers; grafana JSONs have mixed unicode conventions — surgical text edits only, never a full json.dump round-trip), and close out per the ritual: update PLAN.md, write handoffs/HANDOFF_24.md with the complete next prompt + a detailed TODO an older model can follow + fresh suggestions, push without merging, and print the full next-session prompt in the chat.
