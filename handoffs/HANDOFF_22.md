# Handoff 22 — 2026-07-12 (session 22: session-21 merge + pattern mining + fundamentals persistence + deep-dive horizons)

Continues [HANDOFF_21.md](HANDOFF_21.md). Evergreen orientation in [HANDOFF.md](HANDOFF.md); rolling status/roadmap in [PLAN.md](../PLAN.md); structural map in [PROJECT_SUMMARY.md](../PROJECT_SUMMARY.md); design spec in [SPEC.md](../SPEC.md).

## What session 22 did

On branch **`feat/session-22-patterns`** (off `main` @ `06f588c`), worktree `.claude/worktrees/session-22-patterns`. Pushed, **not merged**.

Session start: **the handoff prompt and the repo agreed** — first time in four sessions (no merged-but-unpushed drift). Session ran on **Sunday 07-12**: the s21 merge gate was cleared (user confirmed the deep-dive flip markers; note they eyeballed `track_record_dashboard.json` instead of `predictions_dashboard.json` — the predictions import + `DEEP_DIVE_UID` replacement **carries forward**) and `feat/session-21-cadence-deepdive` was ff-merged → **`main` @ `06f588c`, pushed before Mon 07-13 10:00 UTC — the Mon/Wed/Fri cadence is live for the coming week**. Reddit creds still 0. API credits were topped up mid-session (needed for the dry-run + mining validation).

The slice (user picks: deep-dive verdicts beyond 30d, dashboard-only for now; Claude-driven pattern discovery with a stored, improvable pattern set; fundamentals persistence as its enabler — their "is C the option that aligns?" → yes):

1. **Migrations 006 + 007 — applied to the live DB with explicit sign-off (2026-07-12)**. [006](../migrations/006_add_fundamentals_to_recommendations.sql): `ALTER TABLE recommendations ADD COLUMN fundamentals JSON NULL AFTER sentiment` — each new rec stores the exact `fetch_fundamentals` snapshot Claude saw (equities only; NULL for ETFs/index/untyped and all pre-s22 rows). [007](../migrations/007_create_prediction_patterns.sql): `prediction_patterns` (generated_at, horizon_days, patterns JSON, narrative TEXT, stats JSON; **append-only** — newest row = current truth, older rows = audit trail of pattern evolution).
2. **Fundamentals persistence**: `write_recommendation` gained a `fundamentals` kwarg ([src/persistence/writers.py](../src/persistence/writers.py) — `json.dumps` when present, NULL otherwise, never an empty placeholder); [src/main.py](../src/main.py) 6c passes `ticker_data["fundamentals"]`. Unlocks the fundamentals-vs-verdict slice when s20-informed calls mature (~2026-08-13).
3. **Pattern miner (main step 11 — Fridays or `--force-patterns`, +1 Haiku call ≈ $0.01/week)**: [src/db.py](../src/db.py) `get_outcome_features` (every graded outcome at a horizon joined to call-time features: action/confidence + technical JSON extracts rsi/price/sma50/pos_52w/volume_ratio + sector/quote_type + fundamentals extracts trailing_pe/dividend_yield_pct) and `get_latest_patterns` (newest row). [src/analysis/patterns.py](../src/analysis/patterns.py) buckets rows across 12 dimensions (accion, confianza, rsi, precio_vs_sma50, posicion_52w, volumen, tipo, sector, pe, dividendo + crosses `accion_x_rsi`, `accion_x_tipo`; unknown → "(sin dato)"; hit rate = C/(C+I) like every panel). `generate_pattern_analysis` ([src/analysis/claude_client.py](../src/analysis/claude_client.py), `_PATTERNS_SYSTEM`) renders the aggregates + **Claude's own previous pattern set** and returns structured output: `patterns[]` (name/description/evidence/status **NEW|CONFIRMED|REVISED|RETIRED**/confidence) + `narrative` (Spanish markdown). `write_prediction_patterns` appends. Failure never kills the run; zero graded outcomes → clean skip.
4. **Digest panels 15/16** ([grafana/daily_digest_dashboard.json](../grafana/daily_digest_dashboard.json)): panel-15 "Patrones de acierto (minería semanal, 30d)" — the newest row's patterns via **`JSON_TABLE`** (first use in this repo; live-probed), status color-mapped (NEW blue / CONFIRMED green / REVISED orange / RETIRED red), ordered by status then confidence; panel-16 — the latest narrative. Neither respects the time picker (always the newest set). Empty until the first Friday mining run post-merge.
5. **Deep-dive Call history 90d/1y columns** ([grafana/ticker_deep_dive_dashboard.json](../grafana/ticker_deep_dive_dashboard.json)): `o90`/`o365` LEFT JOINs on `recommendation_outcomes`, verdict + return columns styled like the 30d pair; description + panel-4 how-to-read updated (30d stays the headline; first 90d ≈ 2026-08-15 — also fixed panel-4's stale "Oct 2026", first 1y ≈ 2027-05-17). **The user's request for a real 180d (6m) grading horizon is pinned to the backlog** (new `HORIZON_BANDS` entry ≈ ±10/21/25% by the √time rule; pipeline change + tests + dry-run + evaluator rows accumulate going forward).
6. **Docs**: PROJECT_SUMMARY (tables incl. `prediction_patterns`, module map + `patterns.py`, step 6c/11, "writes to all 9 owned tables", per-run call count), README (`--force-patterns`, migrations 001→007, digest + deep-dive blurbs), PLAN refresh, this handoff.

## Validation evidence

- **pytest: 98 passed** (84 + 14 new: [tests/test_patterns.py](../tests/test_patterns.py) — bucket edges, aggregation with ETF/(sin dato)/crosses + JSON-safety, prompt carries aggregates + previous set + first-run wording, status enum in schema, main() wiring Friday/off-Friday/forced/failed/no-outcomes; [tests/test_writers_dedup.py](../tests/test_writers_dedup.py) — fundamentals JSON vs NULL params, patterns INSERT + dry-run). `test_main_resilience`'s `write_recommendation` stub gained the `fundamentals` kwarg.
- **Full dry-run through the real batch API: 63 ok / 0 failed at $0.1013**, summary MIXED. The first `--force-patterns` attempt surfaced a real API restriction — **structured output rejects `minimum`/`maximum` on `number` schema properties (400)** — and the resilience path held exactly as designed (mining failed, run completed green). Schema fixed (range stated in the prompt instead).
- **Mining re-validated ad-hoc against the live API + DB** (same inputs main uses: 1,134 feature rows): **8 patterns, all NEW (first run), evidence citing exact buckets** — HOLD×ETF 93% (99 decided), **WATCH×ETF 14% (244 decided)** — a genuinely actionable find echoing the s18 ETF concern, confidence 0.60–0.79 sweet spot 58% vs <0.40 at 25%, RSI<30 33% (21 decided, small-sample caution correctly expressed in its confidence). Narrative honest about the 51% global rate.
- **Live rawSql green** (collation init_command): JSON_TABLE syntax probe; digest panels 15/16 (0 rows on the empty table, no error); deep-dive panel-8 multi `'AAPL','MU','XESC.DE'` (75 rows) / single (25) / unknown (0), matured 30d verdicts appear on a May–June window (AAPL 18 graded), 90d/1y columns NULL as expected. Both JSONs parse; digest diff purely additive (287 insertions).
- **Migrations verified applied**: `SHOW COLUMNS` has `fundamentals` after `sentiment`; `prediction_patterns` exists.

## Invariants (don't break)

All of HANDOFF_21's invariants stand (Friday stays in the cron — retro **and now pattern mining** both key on it; flips annotation keeps `filter.ids: [5]`; `DEEP_DIVE_UID` placeholder is not a bug; flip counts read higher under Mon/Wed/Fri gaps; never write to stock-snapshots tables; `--dry-run` = no writes, real API; Spanish prompts; no persisted placeholders; 30d headline; `get_latest_actions` before 6a; ETF/fundamentals fetches mutually exclusive by quote_type; `dividendYield` percent units; schema-v2 rules — no `timeSettings.weekStart/nowDelay/quickRanges`, no `vizConfig.spec.version`; batch custom_ids `[A-Za-z0-9_-]`; worktree-per-session; validate with the main checkout's `.venv`). New this session:

- **Structured-output schemas must not use `minimum`/`maximum` on numbers** — the API 400s. State ranges in the prompt.
- **`prediction_patterns` is append-only** — never upsert/overwrite; the evolution is the feature. The newest row is what panels 15/16 and the next mining call read.
- **The miner must always receive the previous pattern set** (`get_latest_patterns`) — feeding it nothing resets the learning loop.
- **`recommendations.fundamentals` is NULL when nothing was fetched** — never `{}` or `null`-strings; `get_outcome_features` relies on JSON_EXTRACT returning SQL NULL for absent rows.
- Digest panels 15/16 are deliberately **not** time-picker-scoped.

## State of play / caveats

- **`main` = `06f588c`** (s21 merged + pushed Sun 07-12, before Mon 10:00 UTC — **Mon/Wed/Fri cadence live**). Session 22 on `feat/session-22-patterns`, pushed, **awaiting merge**.
- **Merge gate for s22 is light**: migrations already applied; the user imports the two touched dashboards (digest panels 15/16 — legitimately empty pre-Friday; deep-dive Call history 90d/1y columns — empty until horizons mature) and approves. **Merge before Fri 07-17 10:00 UTC** or Friday's run won't mine patterns. Optional offer: seed `prediction_patterns` with one real ad-hoc mining run pre-Friday so panels 15/16 render non-empty at import (~$0.01; the validated script pattern is in this session's history — features → summarize → mine → `write_prediction_patterns`).
- **Predictions dashboard still unfinished from s21**: not yet imported by the user; `DEEP_DIVE_UID` still a placeholder in the JSON. If the user supplies the real uid, commit it.
- **Mon 2026-07-13 run** = first under the new cadence with s16+s17+s18+s20 live: 63 ok, cost ≈ $0.097, flips vs 11 on 07-10, fundamentals citations in EQUITY reasonings; confirm **no Tue/Thu runs**.
- **Fri 2026-07-17 run** = first real retro row **and (if s22 merged) the first production patterns row** — the mining prompt will then carry this week's 8-pattern set as "previous", so expect CONFIRMED/REVISED statuses, not all-NEW.
- **WATCH×ETF at 14% over 244 decided** is the standout mining find (HOLD×ETF is 93%): if it persists across weekly minings, a prompt-side intervention (e.g., an explicit caution for WATCH calls on ETFs) is a strong future slice.
- Carried: ETF bucket 37% baseline (track-record panel-6); 252 pre-price_checks candidates ungradeable; price_checks 06-30→07-08 gap permanent; 90d/365d series fill ~2026-08-15 / 2027-05-17; deep-dive price history starts 2026-06-12; price_checks now 3×/week (exit prices up to 2 days off maturity); Reddit dark (`grep -c '^REDDIT_' .env` = 0, 2026-07-12).
- **Local runs**: `env -u ANTHROPIC_API_KEY` (empty shell var shadows `.env`); worktrees have no `.venv` — use `/home/guillo/Git/stock-recommendations/.venv/bin/python`; ad-hoc DB scripts need `load_dotenv('/home/guillo/Git/stock-recommendations/.env')`, `DB_HOST/DB_PORT/DB_USER/DB_PASS/DB_NAME`, and init_command `SET collation_connection = utf8mb4_unicode_ci` for dashboard rawSql. Substitute `$ticker` with a quoted CSV and `$__timeFilter(...)` with a literal BETWEEN.
- **User timezone: Europe/Madrid.**

## Detailed TODO for session 23 (step-by-step; follow in order)

**Step 0 — Orient.** Read this file and [PLAN.md](../PLAN.md). From the main checkout: `git pull`, `git log --oneline -3 --decorate`, compare against the prompt — **if they disagree, the repo wins; say so** (s19/s20/s21 all found drift; s22 did not — check anyway). `git worktree list`; `git status --short` in the newest worktree for interrupted work.

**Step 1 — Merge gate for session 22.** Ask the user (AskUserQuestion) to import from the s22 branch/worktree: (a) `grafana/daily_digest_dashboard.json` — panels 15/16 appear at the bottom (empty is correct pre-Friday; offer the ad-hoc seeding run if they want them populated now); (b) `grafana/ticker_deep_dive_dashboard.json` — Call history shows `90d verdict/return` and `1y verdict/return` columns (empty is correct). On approval: `git checkout main && git merge --ff-only feat/session-22-patterns && git push`. Remind: **merge before Fri 07-17 10:00 UTC** so Friday mines patterns. Also chase the s21 leftover: import `predictions_dashboard.json`, replace `DEEP_DIVE_UID` (segment after `/d/` in the deep-dive URL), confirm symbol click-through; commit the real uid if provided.

**Step 2 — Verify the week's production runs.** `gh run list --workflow=run_recommendations.yml --limit 5`. Mon 07-13 (and Wed 07-15): 63 ok / 0 failed; cost ≈ $0.097; flips line (2-day-gap caveat — judge trend, not daily-era absolutes); spot-check 2–3 EQUITY reasonings for P/E/margin/growth citations (ETFs should cite exposure/costs). **Confirm no Tue/Thu runs fired.** Record in PLAN.

**Step 3 — First production Friday (on/after 2026-07-17).** Verify: (a) retro — log lines + `SELECT week_start, LEFT(retrospective,200) FROM weekly_retrospectives` + digest panel-12; (b) **patterns (if s22 merged)** — "Mining prediction patterns..." + "Inserted prediction patterns row" in the log, `SELECT generated_at, JSON_LENGTH(patterns) FROM prediction_patterns ORDER BY id DESC LIMIT 1`, digest panels 15/16 render, and **statuses should no longer be all-NEW** if a seeded row existed (the loop is feeding itself). Note whether WATCH×ETF stays ~14%.

**Step 4 — Check Reddit creds** (`grep -c '^REDDIT_' .env`). If >0: GitHub secrets, one real cycle, `reddit_mentions`/`trending_tickers`/panel-14, batched sentiment becomes buildable.

**Step 5 — Pick the next slice with the user** (AskUserQuestion). Suggested order:
1. **Real 180d (6m) grading horizon** — user-pinned in s22: add `HORIZON_BANDS[180]` ≈ ±10/21/25% (√time interpolation; get band sign-off), evaluator picks it up via `HORIZONS`, dashboards gain the column where 7/30/90/365 already show (deep-dive hit-rate-by-horizon, trend charts), tests + full dry-run. First 180d verdicts for existing recs mature retroactively as `evaluate_outcomes` runs (earliest rec 2026-05-17 → first 180d ≈ 2026-11-13; no re-grade needed — new horizon rows only).
2. **Per-run cost telemetry in the DB** (migration — sign-off): persist the run's token/cost line; cost-trend panel confirms the −40% cadence saving.
3. **ETF-WATCH intervention** if the 07-17 mining confirms the 14% bucket: prompt-side caution for WATCH on ETFs (cheap, prompt-only, dry-run evidence).
4. **Benchmark-relative + dividend-adjusted grading** (~2026-08-15, needs decisions + re-grade sign-off), portfolio lens dashboard, flip-stability round 2 (only if the week says so), batched Reddit sentiment (if creds).

**Step 6 — Validate the standard way.** `env -u ANTHROPIC_API_KEY /home/guillo/Git/stock-recommendations/.venv/bin/python -m pytest tests/ -q` (expect **98+**); full dry-run (63 ok / 0 failed; add `--force-patterns`/`--force-retro` if those paths were touched) **only if pipeline code changed**; re-extract touched dashboard rawSql and run live (collation init_command; `$ticker` → quoted CSV; `$__timeFilter` → BETWEEN); migrations only with sign-off. Remember: no `minimum`/`maximum` in structured-output schemas.

**Step 7 — Close out per the ritual.** Update PLAN.md; write `handoffs/HANDOFF_23.md` (complete copy-pasteable next prompt + detailed TODO an older model can follow + fresh suggestions); commit; push the branch (no merge unless asked); print the full next prompt in chat.

## Fresh suggestions (beyond the committed backlog)

- **Seed `prediction_patterns` before Friday** (step 1 offers it): the Friday run then does CONFIRMED/REVISED refinement instead of a cold start, and the user sees panels 15/16 populated at import.
- **Patterns → prompt feedback loop** (the natural next evolution of the user's ask): inject the top CONFIRMED patterns into the per-ticker prompt ("El sistema históricamente falla en WATCH sobre ETFs…") so mining findings change behavior, not just reporting. Needs care: a self-reinforcing loop can entrench bias — gate on CONFIRMED status + high confidence + large samples, and measure before/after hit rates.
- **Commit the real deep-dive uid once known** (carried from s21) — kills the placeholder caveat in three doc spots.
- **Retire the daily-era flip baselines** once two Mon/Wed/Fri weeks exist (recompute panel-13 expectations; carried).
- **Retry-once on transient yfinance timeouts** (carried; cheap, stub-tested).

## Prompt for the next session (copy-paste exactly)

> Read handoffs/HANDOFF_22.md and PLAN.md before doing anything — HANDOFF_22 has the detailed step-by-step TODO for session 23; follow it in order, and first cross-check this prompt's claims against git log (if they disagree, the repo wins — say so; s19–s21 all found drift, s22 didn't but check anyway). Context: main is at 06f588c (session 21 cadence+deep-dive merged + pushed Sun 2026-07-12, before Mon 07-13 10:00 UTC — the Mon/Wed/Fri cron is live; confirm no Tue/Thu runs this week). Session 22 (Sunday 2026-07-12, branch feat/session-22-patterns, pushed NOT merged) delivered the user's pattern-discovery ask + fundamentals persistence + deep-dive multi-horizon verdicts: migrations 006 (recommendations.fundamentals JSON — each new rec stores the snapshot Claude saw, equities only) and 007 (prediction_patterns — append-only, newest row = current truth) are ALREADY APPLIED to the live DB with sign-off; main step 11 (Fridays or --force-patterns) buckets all graded 30d outcomes into 12-dimension hit-rate aggregates (src/analysis/patterns.py) and one Haiku call refines its own previous pattern set (statuses NEW/CONFIRMED/REVISED/RETIRED + evidence + confidence, Spanish narrative) → write_prediction_patterns; digest panels 15/16 show the newest set (JSON_TABLE, status color-mapped) + narrative; deep-dive Call history gained 90d/1y verdict+return columns (empty until ~08-15/2027-05); the user's real-180d-horizon request is PINNED to the backlog (suggested slice #1). Validation: pytest 98 passed; full dry-run 63 ok / 0 failed at $0.1013; the mining call was re-validated ad-hoc live after fixing a real API restriction (structured output rejects minimum/maximum on numbers — range goes in the prompt) — 8 evidenced patterns incl. WATCH×ETF 14% over 244 decided (HOLD×ETF 93%); all touched rawSql green live; migrations verified applied. Steps: (1) merge gate — user imports daily_digest (panels 15/16, empty pre-Friday is correct; offer an ad-hoc seeding mining run ~$0.01 so they render populated and Friday refines instead of cold-starting) and ticker_deep_dive (90d/1y columns), then ff-merge feat/session-22-patterns to main and push — MUST land before Fri 07-17 10:00 UTC or Friday won't mine; also chase the s21 leftover: import predictions_dashboard.json + replace DEEP_DIVE_UID with the real uid (commit it if provided); (2) verify Mon 07-13 + Wed 07-15 production runs (63 ok, ≈$0.097, flips read higher across 2-day gaps — judge the trend; fundamentals citations in EQUITY reasonings; NO Tue/Thu runs); (3) on/after Fri 07-17 verify the first production retro (log + weekly_retrospectives row + panel-12) AND the first production patterns row (log lines + prediction_patterns row + panels 15/16; statuses shouldn't be all-NEW if a seed row existed; note if WATCH×ETF holds ~14%); (4) check Reddit creds (grep -c '^REDDIT_' .env — 0 as of 07-12). Then pick the next slice with the user from HANDOFF_22 step 5 (suggested: the pinned real 180d horizon — HORIZON_BANDS[180] ≈ ±10/21/25 with band sign-off, no re-grade needed; or per-run cost telemetry with migration sign-off; or an ETF-WATCH prompt intervention if Friday's mining confirms the 14%). Create the session worktree + branch (feat/session-23-<slice>), confirm the task list with the user, batch the work, validate (pytest expect 98+ using the main checkout's .venv — worktrees have none; full dry-run 63 ok only if pipeline code changed, add --force-patterns/--force-retro if those paths were touched; rawSql live with SET collation_connection = utf8mb4_unicode_ci, $ticker → quoted CSV, $__timeFilter → literal BETWEEN; migrations only with user sign-off; structured-output schemas must NOT use minimum/maximum on numbers), and close out per the ritual: update PLAN.md, write handoffs/HANDOFF_23.md with the complete next prompt + a detailed TODO an older model can follow + fresh suggestions, push without merging, and print the full next-session prompt in the chat.
