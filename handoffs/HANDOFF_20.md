# Handoff 20 — 2026-07-11 (session 20: session-19 merge + stock fundamentals in the prompt)

Continues [HANDOFF_19.md](HANDOFF_19.md). Evergreen orientation in [HANDOFF.md](HANDOFF.md); rolling status/roadmap in [PLAN.md](../PLAN.md); structural map in [PROJECT_SUMMARY.md](../PROJECT_SUMMARY.md); design spec in [SPEC.md](../SPEC.md).

## What session 20 did

On branch **`feat/session-20-fundamentals`** (off `main` @ `3cdaf1a`), worktree `.claude/worktrees/session-20-fundamentals`. Pushed, **not merged**.

Session start: **the repo disagreed with the handoff prompt and the repo won** — session 19 was found already ff-merged into local `main` (@ `3cdaf1a`) but never pushed (the prompt said "pushed NOT merged" with main at `4f8f5ad`). Flagged to the user; the push was held until the render gate cleared. The user then **imported `ticker_deep_dive_dashboard.json` and confirmed the Ticker dropdown + all 8 panels render** (the repo's first template variable passed its first import), and `main` @ `3cdaf1a` was pushed. Session ran on **Saturday 07-11** again, so HANDOFF_19 steps 2–3 (Mon 07-13 run, Fri 07-17 retro) stay future — the latest production run is still the verified 07-10 11-flip run. Reddit creds still 0.

The slice (user pick from the long-term backlog: **fundamentals in the per-ticker prompt**, lean field set):

1. **Live availability probe first** (per HANDOFF_19): 12 representative equities via yfinance `Ticker.info` — 11–14 of 14 candidate fields present across US/Paris/London/Milan/OTC. Quirks found and pinned by tests: `trailingPE: Infinity` (KRKNF), **`dividendYield` served in percent units** (MDT 3.43 = 3.43%), margins/growth are fractions, `marketCap` is in listing currency (1GOOGL.MI: EUR), one transient 30s timeout (RGTI). `quote_type` split of the 63 actives: **33 EQUITY / 27 ETF / 1 INDEX / 3 None** (SPY5.PA, VHYL.AS — untyped UCITS ETFs).
2. **`fetch_fundamentals` + `_build_fundamentals`** ([src/collectors/prices.py](../src/collectors/prices.py)): `Ticker.info` → `{trailing_pe, forward_pe, dividend_yield_pct, profit_margin, operating_margin, revenue_growth, earnings_growth, market_cap, currency}`; per-field: non-numeric/non-finite → None; all-numeric-fields-empty → None; any fetch error → None (optional enrichment, never fails the ticker).
3. **"Fundamentales" prompt block** ([src/analysis/claude_client.py](../src/analysis/claude_client.py) `_fundamentals_block` + `_human_cap`): "- P/E: 38.2 (trailing) / 32.8 (forward)", "- Capitalización: 4.6T USD", "- Dividend yield: 0.34%", "- Margen: neto 27.2%, operativo 32.3%", "- Crecimiento interanual: ingresos +16.6%, beneficios +21.8%", headed by *"valoración y rentabilidad del negocio — la tesis de largo plazo debe apoyarse en esto, no solo en el precio"*. Omitted when unknown; missing fields skip their lines. Same optional-block pattern as news/earnings/ETF.
4. **Wiring** ([src/main.py](../src/main.py) step 6a): fetch **only for `quote_type == "EQUITY"`** — mutually exclusive with the ETF block by quote_type; the index and untyped tickers get neither and pay nothing.
5. **Docs**: PROJECT_SUMMARY (step 6a + collectors line), PLAN refresh, this handoff.

## Validation evidence

- **pytest: 84 passed** (75 + 9 new in [tests/test_fundamentals.py](../tests/test_fundamentals.py): `_build_fundamentals` normalization / Infinity-PE drop / non-numeric tolerance / empty→None, block render + omission + partial-lines, `_human_cap` scaling, main() fetches only for EQUITY — the ETF/INDEX/None tickers never trigger it and carry `fundamentals: None` into the batch). `test_etf_info`'s main() harness now stubs `fetch_fundamentals` (its AAPL fixture is EQUITY and would otherwise hit the network).
- **Live smoke**: AAPL (full block), KRKNF (Infinity PE dropped, negative margins render), AIR.PA (EUR market cap) — all correct.
- **Full dry-run through the real batch API: 63 ok / 0 failed at $0.0970** (vs the $0.0973 s18 baseline — the ~5-line block × 33 stocks is lost in output-token variance). 14 flips vs the stored 07-10 production run (several repeat the known consistent cohort: SMCI/MU/SOLS/VWRL.AS/TOYO/AVAV/CLS). Saturday correctly skipped the retro path. Summary MIXED.
- No dashboards touched (no rawSql to validate), no migrations.

## Invariants (don't break)

All of HANDOFF_19's invariants stand (never write to stock-snapshots tables; `--dry-run` = no writes, real API; Spanish prompts; no persisted placeholders; 30d headline horizon; `get_latest_actions` before step 6a; ETF fetch only for `quote_type == "ETF"`; `_build_etf_info` → None when unknown; deep-dive `$ticker` keeps `multi`+`includeAll` and `IN ($ticker)` filters; price-history panel stays `format: time_series`; only the deep-dive has a variable; panel-6 ETF bucket keys on `t.quote_type`; schema-v2 dashboard rules; batch custom_ids `[A-Za-z0-9_-]`; worktree-per-session ritual; user only sees the final message). New this session:

- **`fetch_fundamentals` is only called for `quote_type == "EQUITY"`** — ETFs keep their `funds_data` block instead; the index and untyped tickers get neither. The two blocks are mutually exclusive by quote_type.
- **`dividendYield` stays in Yahoo's percent units** (3.43 means 3.43% — render as `f"{v:.2f}%"`, never `:.2%` which would ×100). Margins/growth ARE fractions (`:.1%` is correct there). Don't "fix" one to match the other.
- **`_build_fundamentals` drops non-finite numbers** (Infinity P/E) per field and **returns None when every numeric field is missing** — the block must stay omitted rather than render empty/bogus lines.

## State of play / caveats

- **`main` = `3cdaf1a` (session 19 merged + pushed, deep-dive dashboard render-verified)**; session 20 is on `feat/session-20-fundamentals`, pushed, **awaiting user merge**. Light gate: no dashboards, no migrations — merge **before Mon 07-13 10:00 UTC** if the week's production data should carry the fundamentals.
- **The Mon 2026-07-13 run** is the first with s16+s17+s18 (+s20 if merged in time) live: expect 63 ok, cost ≈ **$0.097**, and a flip count to compare against 11 (07-10). The Mon→Fri flip trend is the production verdict on session 16; **flip-stability round 2** only if flips stay ≥10 with same-day reversals.
- **First real retrospective row lands Friday 2026-07-17** (log line + `weekly_retrospectives` row + digest panel-12).
- **ETF hit rate 37% (486 outcomes) is the s18 baseline**; watch the ETF row in track-record panel-6. First ETF-informed calls mature ~mid-August; first fundamentals-informed calls (if s20 merges before Monday) mature ~2026-08-13 at 30d.
- **Reddit still dark** (`grep -c '^REDDIT_' .env` = 0, 2026-07-11). Batched Reddit sentiment stays gated.
- **Remaining committed backlog** (PLAN roadmap): lower run frequency (cheap — cron line + user cadence decision), benchmark-relative + dividend-adjusted grading (needs user decisions + re-grade sign-off; best ~2026-08-15 when 90d outcomes mature). Also available: per-run cost telemetry in the DB, portfolio lens dashboard, deep-dive call annotations, predictions→deep-dive data link, flip-stability round 2 (only if the week says so).
- Carried: 252 pre-price_checks matured candidates ungradeable; `price_snapshots` stale since 2026-05-22; price_checks gap 06-30→07-08 permanent; 90d/365d series fill from ~2026-08-15 / 2027-05-17; deep-dive price history starts 2026-06-12.
- **Local runs:** `env -u ANTHROPIC_API_KEY` (empty shell var shadows `.env`). Ad-hoc DB scripts: `load_dotenv('/home/guillo/Git/stock-recommendations/.env')`, env vars `DB_HOST/DB_PORT/DB_USER/DB_PASS/DB_NAME` (don't forget the port), init_command `SET collation_connection = utf8mb4_unicode_ci` when validating dashboard rawSql.
- **User timezone: Europe/Madrid.**

## Detailed TODO for session 21 (step-by-step; follow in order)

**Step 0 — Orient.** Read this file and [PLAN.md](../PLAN.md). From the main checkout (`/home/guillo/Git/stock-recommendations`): `git pull`, then `git log --oneline -3` and compare against the prompt you were given — **if they disagree, the repo wins; say so to the user before doing anything** (it happened this session: s19 was merged-but-unpushed). `git worktree list`; check the newest worktree's `status --short` for interrupted work.

**Step 1 — Merge gate for session 20.** Light gate (no dashboards/migrations): ask the user (AskUserQuestion) to approve merging `feat/session-20-fundamentals`. On approval: `git checkout main && git merge --ff-only feat/session-20-fundamentals && git push`. If it's still before Mon 07-13 10:00 UTC, merging now puts fundamentals into the week's production data — say so. After the first production run with it, spot-check a reasoning or two for fundamentals citations (P/E, margins, growth in the Spanish text).

**Step 2 — Verify the week's production runs (Mon 07-13 onward).** `gh run list --workflow=run_recommendations.yml --limit 5`; per scheduled run check: 63 ok / 0 failed; cost line ≈ $0.097; the `Action flips vs previous run:` count vs 11 on 07-10. Record counts in PLAN. Settling = low single digits without same-day reversals; if flips stay ≥10 with reversals, flip-stability round 2 becomes a candidate slice.

**Step 3 — First production retrospective (on/after Friday 2026-07-17).** After Friday's run: the log shows the retrospective generation + upsert lines; `SELECT week_start, LEFT(retrospective, 200) FROM weekly_retrospectives;` has the week's row; the user reads digest panel-12. Note whether its flip commentary matches step 2's counts.

**Step 4 — Check Reddit creds** (`grep -c '^REDDIT_' .env`). If >0: add the three GitHub Actions secrets, run one real cycle, verify `reddit_mentions` + `trending_tickers` fill and digest panel-14 renders; batched Reddit sentiment becomes buildable.

**Step 5 — Pick the next slice with the user** (AskUserQuestion). Suggested order:
1. **Lower run frequency** (daily → 2–3×/week): ask the user for the cadence; edit the cron line in [.github/workflows/run_recommendations.yml](../.github/workflows/run_recommendations.yml); **keep a Friday run** (the retro trigger). Cuts the API bill proportionally and reduces flip noise. Smallest possible slice — pair it with one of the follow-ups below.
2. **Deep-dive polish pair**: call annotations on the price chart (annotation query on `recommendations` filtered to `$ticker`) + a data link from the predictions per-ticker table (`var-ticker=${__data.fields.symbol}`). Dashboard-only; first-import eyeball as merge gate.
3. **Per-run cost telemetry in the DB**: persist the run's token/cost line into a table (needs a migration — user sign-off) so cost trends become dashboardable.
4. **Benchmark-relative + dividend-adjusted grading at 90d/365d**: needs user decisions (benchmark choice, re-grade or not — session-14 rule) and total-return data; best started once 90d outcomes near maturity (~2026-08-15). Do the two together.
5. Others if asked: portfolio lens dashboard, flip-stability round 2 (only if step 2 says so), batched Reddit sentiment (only if creds).

**Step 6 — Validate** the standard way: `env -u ANTHROPIC_API_KEY .venv/bin/python -m pytest tests/ -q` (expect 84+); full `env -u ANTHROPIC_API_KEY .venv/bin/python -m src.main --dry-run` if pipeline code changed (63 ok / 0 failed; `--force-retro` if the retro path was touched); re-extract and run any touched dashboard rawSql with the collation init_command (substitute `$ticker` with a quoted CSV for deep-dive queries); migrations only with user sign-off. Workflow-only changes (cron): validate the YAML parses and the cron comment matches the line — no dry-run needed.

**Step 7 — Close out per the ritual.** Update PLAN.md; write `handoffs/HANDOFF_21.md` with a complete copy-pasteable next prompt + a detailed TODO an older model can follow + fresh suggestions; commit; push the branch (no merge unless asked); print the full next prompt in chat.

## Fresh suggestions (beyond the committed backlog)

- **Fundamentals-vs-verdict slice on the track-record dashboard**: once fundamentals-informed calls mature (~08-13), a hit-rate-by-P/E-band or by-dividend-payer table would show whether the new inputs actually improve calls — the same "is the lever working" pattern as the ETF 37% baseline. Needs the entry fundamentals persisted first, which they currently are NOT (only the prompt sees them) — persisting a compact fundamentals JSON on the recommendation row would be a small migration (user sign-off).
- **Retry-once on transient yfinance timeouts**: the probe hit one 30s curl timeout (RGTI). Each enrichment fetch degrades to None correctly, but a single retry would cut silent data gaps on flaky days. Cheap, test with a stubbed raiser.
- **Deep-dive annotations + predictions link-through** (from HANDOFF_19, still open): both are one-file dashboard edits and pair well into a half-session slice.

## Prompt for the next session (copy-paste exactly)

> Read handoffs/HANDOFF_20.md and PLAN.md before doing anything — HANDOFF_20 has the detailed step-by-step TODO for session 21; follow it in order, and first cross-check this prompt's claims against git log (if they disagree, the repo wins — say so; it happened in session 20: s19 was merged-but-unpushed). Context: main is at 3cdaf1a (session 19 ticker deep-dive dashboard merged + pushed 2026-07-11 after the user render-verified the first template variable). Session 20 (Saturday 2026-07-11, branch feat/session-20-fundamentals, pushed NOT merged) delivered stock fundamentals in the per-ticker prompt — the first long-term-backlog item: fetch_fundamentals + _build_fundamentals in src/collectors/prices.py (yfinance Ticker.info, lean set: trailing/forward P/E, dividend yield kept in Yahoo's percent units where 3.43 means 3.43%, net+operating margins and revenue/earnings growth as fractions, market cap humanized with currency; non-finite values like KRKNF's Infinity P/E dropped; None on error/empty — never fails the ticker), a "Fundamentales" Spanish prompt block (_fundamentals_block in claude_client.py) telling the model the long-term thesis must rest on the business not just price, wired in main.py 6a ONLY for quote_type == "EQUITY" (33 of 63 actives; ETFs keep their s18 composition block — mutually exclusive; the INDEX + 3 untyped tickers get neither). Availability was probed live first: 11-14 of 14 fields across US/EU/OTC equities. Validation: pytest 84 passed (9 new in tests/test_fundamentals.py); full dry-run 63 ok / 0 failed at $0.0970 (≈ s18's $0.0973 — no measurable cost delta); no dashboards, no migrations. Steps: (1) merge gate — light, user approval then ff-merge feat/session-20-fundamentals to main and push; if before Mon 07-13 10:00 UTC the week's production carries it — say so; after the first production run spot-check reasonings for fundamentals citations; (2) verify the week's production runs (Mon 07-13 onward: 63 ok, cost ≈$0.097, flip trend vs 11 on 07-10 — the verdict on session 16; flip round 2 only if flips stay ≥10 with same-day reversals); (3) on/after Fri 07-17 verify the first production weekly retrospective (log line + weekly_retrospectives row + digest panel-12); (4) check Reddit creds (grep -c '^REDDIT_' .env — 0 as of 07-11). Then pick the next slice with the user from HANDOFF_20 step 5 (suggested: lower run frequency — cron line + user cadence decision, keep a Friday run for the retro — ideally paired with the deep-dive polish pair of call annotations + predictions→deep-dive data link; also available: per-run cost telemetry with migration sign-off, benchmark-relative + dividend-adjusted grading once 90d outcomes near maturity ~08-15, portfolio lens, batched Reddit sentiment if creds). Create the session worktree + branch (feat/session-21-<slug>) once the slice is known, confirm the task list with the user, batch the work, validate (pytest expect 84+, full dry-run 63 ok if pipeline code changed, rawSql with SET collation_connection = utf8mb4_unicode_ci if dashboards touched — substitute $ticker with quoted CSV on deep-dive queries, migrations only with user sign-off), and close out per the ritual: update PLAN.md, write handoffs/HANDOFF_21.md with the complete next prompt + a detailed TODO an older model can follow + fresh suggestions, push without merging, and print the full next-session prompt in the chat.
